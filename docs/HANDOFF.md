# handoff

This document is for whichever Claude session picks the project up next. **It is the rolling source of truth** for project state, recent decisions, and what's queued. Edit it as the project evolves; archive snapshots if you want history (or rely on git).

If you're a Claude reading this for the first time in a new session: read this entire doc, then `BACKLOG.md`, then skim `ARCHITECTURE.md` if relevant to what Daniel is asking about. `CHANGELOG.md` is per-version detail you usually don't need.

## who & what

Daniel Nelson is building a browser-based kaleidoscope tool for high-resolution still-image output. He's a VJ (Resolume Arena + Akai APC40 MK2), technically savvy but identifies as a non-developer. He's iterative, evidence-based, and methodical — runs builds locally, reports back specifically with what works and what doesn't, catches Claude's UI hallucinations.

He prefers **no em dashes** in any prose Claude generates for him.

## current version

`v0.12.6 · Build 264`. The footer shows this from `src/version.js`.

**DANIEL'S B262/B263 VERIFY (2026-07-08): "looking and working great", keyboard reliable so far.** But **the Brave first-load blank panel is NOT fixed by B262** — back at the TOP of triage with datapoints + next steps (needs Brave instrumentation; no more blind guesses). **B264 shipped his prioritized refinements:** vertical-middle parity (per-panel bottom spacers via `equalizeStageMiddles` — all pictures share one true middle), the live divider's 50/50 snap (~24px zone), hint copy "switch to PiP overlay", footer control "PiP". **His new directives (2026-07-08, this round):** (1) parallel-lane pickup starting with the DROSTE affordance + the related onion-skin bug (trailing ghosts draw a complete circle instead of the actual segment shape of the current view — fix both in one pass); (2) +gesture in motion is unblocked (backlog item updated with the capture-tap sketch); (3) bounce PLAYBACK mode in motion (filed, directed); (4) a detailed IxD spec for "stage changes" live keyframe editing while playing — PROPOSED in BACKLOG → Open animation threads, awaiting his react (mirrors perform's stage/take/cut + panels + follower; two playheads on the timeline; ghost markers for staged diffs; slim staging bar with take/cut/discard). (5) **He has a DMG ready for the Arena/M1 Max hop** — the feature-complete gauntlet was handed to him in chat (Syphon smoothness under perform transitions, pause-on-blur, memory-pressure reading for the Arc 6 gate, soak run).

**B263 SHIPPED THE THREE-PANEL LIVE LAYOUT (Daniel's full directive, auto-dock included).** The live view toggles between the PiP overlay (default) and a real third sibling panel (`#livePanel` in the stage split; `#livePip` re-parents into it via the srcScrub pattern, canvas keeps its GL context, aspect-fit at a raised 1600px cap). Three ways in per his spec: the footer [pip][panel] view selector (lower right), a hover dock button over the PiP, and the far-side grippy that shows in BOTH perform layouts (pulling it out in PiP opens the panel from zero — `setupLiveDivider` in controls.js mirrors the stage divider). Auto-dock landed too: dragging the panel under ~300px overlays "release to move live to the PiP overlay" and releasing transitions to PiP. `session.performLayout` + `session.stageLivePct` persist for the session. **With this, Arc 5's layout work is done; the only Arc 5 item left is saved next-up snapshot slots (the stage library) — then the feature-complete checkpoint WITH Daniel (Electron/Arena hop + first M1 Max + memory-pressure reading, gating Arc 6 MIDI).** Awaiting his verify: the B263 list in CHANGELOG, plus B262's keyboard fix (space/S/T after touching any slider — his one outstanding bug, now root-caused) and the Brave first-load fix.

**DANIEL'S ROUND-3 VERIFY (2026-07-08, post-compact): "amazing. everything is better."** Broadcast output is CLEAN in all browsers now (Firefox still slightly less smooth than the others but no longer broken — the residual is the known per-frame ceiling, watch at the M1 Max checkpoint). His one outstanding bug: **space/S/T still unreliable until he pressed take in the UI once** — ROOT-CAUSED + FIXED IN B262: the key handlers guarded against ANY focused INPUT, and every slider keeps focus after a click, so touching any slider silently ate the keys; take "fixed" it only because the footer delegate blurred it. Now only text-entry fields keep the keyboard (perform + motion both); buttons blur before handling; selects blur after use in perform; entering perform clears focus. **B262 also root-caused + fixed the Brave first-video-load blank source panel** (Brave blocks muted autoplay → play() rejected → the park's seek-to-0 was a same-time no-op → no frame ever PRESENTED on Blink; a never-played video now parks 10ms in, forcing a real seek/presentation). Safari camera contention stays backlogged as scheduled (he confirmed). **He judges Arc 5 mostly done; his directive for the next pass (2026-07-08): the THREE-PANEL live layout** — a third panel for the live output that can be toggled INSTEAD of the PiP overlay; the [PiP][three-panel] selector in the perform footer's lower right (already spec'd); a dock icon button over the PiP on hover; if the user drags the live panel below ~300px, show informational text ("move to PiP overlay") and auto-transition to PiP; RETAIN the grippy on the far side so they can pull the panel back out (a third way back to three-panel, besides the PiP button and the footer control). Auto-dock is the LOWEST priority of these — punt if budget requires; the primary UI + interactions come first.

**DANIEL'S THIRD PERFORM ROUND (2026-07-08, "gone from functional to feeling mostly there") — ALL LANDED IN B261:** the broadcast-window jerk (his "closest to a showstopper") = the known Firefox popup-rAF-throttling item, fixed by making the STATE MESSAGE the popup's render clock (rAF demoted to a 100ms fallback — output-view.js); real caret + the fly-away fix (`.ot-btn:hover` background shorthand wiped the image; longhands re-pinned); transition speed max → 10s (and confirmed to him: the seconds value is the follower's RESPONSE time constant, moves are perceptually normalized by the spring); mode select + footer buttons now BLUR so space/S/T always land (his S-went-to-still = native select type-ahead); the video time ruler is BACK above the footer timeline showing EFFECTIVE duration (clip ÷ speed — removing it was an oversight, not a decision); reset canvas no longer resets frame aspect (his model: canvas settings are sub-attributes of the aspect; also kills the mid-broadcast desync); mid-perform source switches refresh the footer center (identity-guarded env.refreshPerformSource from arrangeSlots — fixes his stale video label / missing timeline both directions); still sources show a thumb + name · dims placeholder in the center (his suggestion; the engine-capture tween strip is NOT trivial → moved to the Parallel lane pending conviction). **New triage items filed:** Brave first-video-load blank source panel; Safari main-vs-window camera contention CONFIRMED still open (the long-standing top-of-triage starve item, not a regression). **He verified everything else from rounds 1–2. Perf note: camera sources still jerk slightly more than stills (per-frame texture upload cost) — the known ceiling, watch at the M1 Max checkpoint. HE COMPACTS AFTER THIS PASS.** **VERSIONING PROCESS (Daniel, 2026-07-08): patch bump per build; MINOR bumps are reserved for major feature milestones he'd recognize (perform mode itself = yes; hold/take/cut = should have been a patch — v0.12.0 was over-eager, no harm, continue with patches).**

**DANIEL'S SECOND PERFORM ROUND (2026-07-08, "mostly tuning") — LANDED IN B259/B260:** carets on the dropdowns; frozen-camera button says "live" not "record" (record = to-disk only); **vocabulary locked: "stage" (was hold) · "take" (his "blend in") · "cut" (kept, he hadn't envisioned it, no harm)** — his confirmed mental model: stage = gestures staged without live follow, take = commit the blend, stage stays on after take; **staging onion skin** = ONE steady ghost of the prior (held live) state beside the staged wedge while staged (the side-by-side read), animated trail only on the take, overall opacity cut ~45%; **dot spec clarified: amber differing · GREEN when in-sync AND broadcasting · hidden when in-sync without broadcast**; "staged" label on the stage panel counters the PiP's "live"; **keyboard: space = stage then take (space is free in perform — play/pause is a button here), S/T explicit**; **the perform footer rebuilt to his directive layout** (LEFT: stage wide / take|cut / play wide in motion's geography, play disabled-not-hidden for non-video; CENTER: the video timeline re-parented full-size w/ looping playhead + scrub, camera icon+name for live, still = empty pending the staged-tween strip; RIGHT: transition speed, playback speed, future layout selector). **CCW-rotation-goes-CW: he can't repro post-B254 (lead cap likely fixed it) — WATCH ITEM, ask on next validation.** **Still open from his rounds (specs in BACKLOG):** the still-source center strip (current state left → staged right with tween cells, NO time ruler); the PiP ↔ three-panel layout selector (lives right column; three-panel shares perform mode); saved next-up snapshot slots; X-offset fix (B257) needs his Firefox+webcam retest (+ the still-image datapoint if it persists); Electron/M1-Max checkpoint at feature-complete (he'll be pinged). **B257 = the output-window X-offset fix** (the popup's second camera capture now matches the main capture's negotiated mode; if a STILL also offsets in the window, the hypothesis is wrong — Daniel to report). **B258 = ARC 5 CORE (v0.12 minor bump): hold · take · cut** in the perform footer — hold gates the follower's target feed (stage edits off-air, discrete included), take commits the stage once at the transition speed and STAYS held (repeatable takes), cut snaps; the in-sync dot now measures live-vs-stage divergence directly. **Arc 5 remaining:** three-panel layout option (PiP default; ship with a layout toggle), saved next-up snapshot slots (stage library), PiP auto-dock polish. **Then the feature-complete checkpoint WITH Daniel** (Electron/Arena hop + first M1 Max + memory-pressure reading — gates Arc 6 MIDI). **B255 shipped the perform footer** (video loops while performing w/ play/pause + 50–200% speed; transition speed relocated out of canvas settings; frame picker returns while paused). **B256 shipped the center mode DROPDOWN** (shows current mode; hidden segmented buttons remain the wiring substrate it proxies) **+ PiP polish** (24%, radius-2xl, sync dot hides when caught up — green competed with broadcast green). **DANIEL'S FIRST PERFORM SESSION (2026-07-08): "huge milestone", plus a feedback round being worked through builds 254+:** ✅ B254 fixed the stuck-spin bug (follower lead capped at one lap), the truncated/strobing onion skin (full-transition trail, ghosts painted inside every overlay draw via view.performGhosts), and Firefox's PiP toggle over the source (disablePictureInPicture everywhere). **Still queued from his round:** output-WINDOW X-offset on webcam (~30% right; hypothesis = the popup's second getUserMedia negotiates a different aspect than the main capture → pass the main capture's actual dims as constraints); perform needs VIDEO PLAYBACK (play/pause + speed — his most common use case is performing over a video loop) + the transition-speed control moved out of canvas settings into a perform control cluster; mode selector → CENTER of the app bar as a dropdown (show current mode, not all three); PiP smaller % + radius-2xl corners; in-sync indicator: green competes with broadcast-green → hide the dot when in sync (his suggested option, agreed); three-panel layout option = Arc 5 evolution (PiP stays default, both share perform mode — confirmed to him). **Then the Arc 5 pass he authorized: hold/take/cut transport on the follower** (hold = stop feeding targets = edit off-air; take = resume = the spring blends live→stage at the transition speed; cut = follower.jump). Checkpoint w/ Daniel at feature-complete. **PERFORM MODE IS LIVE (B252, the v0.11 minor bump)** — third mode segment; the output panel is the STAGE (edits render immediately), the live PiP + all broadcasts ease toward it via the follower; `env.programState()` is the "what the audience sees" seam (bus + output window consume it; still/motion unchanged, broadcast works from every mode); transition-speed control under the output panel (session.performResponse); first-cut in-sync dot on the PiP (accent chasing / green synced); perform accepts live sources, save hides, render stays motion-only; force-exits when the source clears; PiP-GL-failure degrades to broadcasts-only. **Daniel's mode-collapse note (2026-07-08): likely still|motion|perform collapses back together after perform-specific adaptations mature — the separate mode is the SPACE to get live-performance exactly right, not necessarily the end state. Hybrid cases parked: perform-from-a-looping-motion (alpha testers expect scrubber-position edits to stick / apply at the scrubbed point), autoplay with variable per-loop shifts (show what auto is moving toward? user-specified up-next states?). Broadcasting from either mode = supported regardless (done via programState).** **Arc 4 sequence: 1 ✅ follower (B251) → 2 ✅ perform mode + PiP + seam (B252) → 3 onion-skin wedge ghost (older samples nearly invisible, trail fades as live catches up) → 4 in-sync affordance refinement + PiP polish (drag/resize?) → feature-complete checkpoint WITH DANIEL (Electron/Arena + first M1 Max + memory-pressure reading — he asked to be pinged rather than testing Electron early).** **Rounds 6+7 feedback fully landed in builds 247–250** (keyframe context menu + ghost select + locked duration + arrows / panel polish / footage thumbs in the still frame picker / row coherence at 40px + compact 32px timeline icons + 25% speed omitted pending interpolation + the full keyboard set with 0=fit).

**ARC 4 IS GO — Daniel's design answers (2026-07-07):** (1) **perceptual follow ramp first** (state-delta-normalized durations; literal as revert/option). (2) **Ghost = wedge overlay outline, onion-skin style** — older samples nearly invisible, the tween trail fades as the realtime output catches up; NOT raster-over-raster (hard to read). (3) **The THIRD PANEL comes FORWARD into Arc 4** (was Arc 5): output-preview is the stage for what's about to happen, output-live shows what the audience sees, **PiP is the practical default layout** — this also resolves the canvas-edit-while-live question (edits preview on the staged panel). (4) Wants a **visual affordance for when live and staged are showing exactly the same thing** (in-sync indicator). (5) **Winding:** gesture capture is the default; with a slow follow + multi-param fine-tuning, the output should bypass interstitial inputs and chase the MOST RECENT input directly (target-chasing) — the mental model: perform with either regular inputs or gestural inputs, translating between +keyframe and +gesture. (6) He sees this **unlocking +gesture in the normal motion editor via shared logic.** (7) **Electron/rig testing waits until feature-complete — check in with him then.** **ACTIVE PROGRAM: the UX restructure → perform program** (Daniel's 2026-07-06 design drop, a couple days of sketching + informal usability sessions, sequenced WITH him into arcs 0–7). **Executable plan: `~/.claude/plans/i-m-previous-sessions-we-ve-indexed-frog.md`** (supersedes `fold-live-perform-phase-1-2.md`; the sparkle spec's architecture decisions #1–8 still hold). The arc digest lives in `BACKLOG.md` → Fold Live section. **Sequence:** 0 bugs → 1 mode discipline + global app bar (his spec: +upload w/ filename under · camera dropdown + record/pause toggle · Still|Motion mode selector · undo/redo · per-mode export controls [motion: broadcast+render / stills: save] · Fold v# chip) → 2 panel-sibling overhaul (source/output symmetric, parallel form/canvas control stacks, placement via 2–3 in-app variations Daniel reacts to) → 3 timeline rework (tall track, fat left transport, right icon stack) → 4 realtime perform (ghost/echo + follow ramp, unwrapped winding) → 5 staged transitions (output-staged third panel, blend-in on the shared speed control, live-as-PiP) → 6 cut line, memory-pressure governed (MIDI then Syphon-IN) → 7 mobile perform (always-PiP, record WITH audio).

**DANIEL'S ROUND-5 FEEDBACK (2026-07-07) — PLACEMENT DECIDED: PROGRESSIVE DISCLOSURE.** Both 2b variations (below AND flank) rejected even accounting for polish; his spec: **persist the essentials under each visual** (source: resolution/duration/filename/frame scrubber + the form selectors with reset slice under them; output: frame aspect [renamed from "frame"] + reset canvas) and put the dense controls behind a **settings control beside the selectors that opens a POPOVER** (source: slice/segment controls in a single column; output: zoom/rotation/out-of-bounds). Also from his pass: scrubber should read as a real mini TIMELINE (not a slider); iPad source-overflow scaling bug; the divider grippy still finicky on touch. He's away; authorized autonomous continuation through these notes and into ARC 3. **BUILD 242 shipped the three fixes** (panel content wraps measure their true free space + `.stage-panel` overflow guard; 10px grippy w/ 4×48 grip + wider touch extenders; the frame picker is now a 36px track + vertical playhead line). **BUILD 243 SHIPPED the progressive-disclosure restructure:** source panel persists scrubber/meta/form-picker+trigger/reset-slice with the slice controls in `#slicePopover`; output panel persists **frame aspect** (renamed) + reset-canvas with zoom/rotation/OOB in `#canvasPopover`; `?panelctl` + all `.ctl-stack` CSS deleted; all control ids preserved. Popovers = fixed-position body-level, anchored above the gear, one at a time, outside-click/Escape close. **BUILD 244 SHIPPED the ARC 3 CORE (timeline rework):** tall track (~236px footer, controls off the top), left fat cluster (+keyframe/+gesture · prev/next · wide play + a quiet delete/anchor row — my placement call, flag if wrong), right icon stack (⋯ − ＋ ⛶), ⋯ overflow = duration/smoothing/labeled loop toggle/speed presets (now w/ 150%)/edit clip/motion JSON, **render moved to the app bar** (per-mode export per the spec), keyboard space/delete, kf0 undeletable, thumbs 160². Builds 242/243/244 all await Daniel's verify (lists in CHANGELOG). **BUILD 245 SHIPPED 2d aspect expansion:** 1:1·5:4·4:3·3:2·16:9, landscape default, click-the-selected-again flips portrait (label follows, ⇅ hover affordance, 1:1 exempt); every dims consumer already handled portrait (verified). **DANIEL'S ROUND-6 FEEDBACK (2026-07-07, testing on localhost — Vercel stopped deploying after 241; repo/GitHub are fine, it's the Vercel dashboard to check):** overall "looking great". Directions landed: keyframe ops = CONTEXT MENU on the keyframe (B247: select/right-click → anchor position / auto space / red delete; delete+anchor row removed, footer trimmed); ghost-at-end now selects kf0 (B247, his "can't select first keyframe from the end"); video duration field reads locked (B247); still-scrubber should render REAL FRAMES like the motion timeline (build if cheap, else backlog); form selectors in motion = disabled not hidden; output-row icon heights must match (gear vs aspect buttons); reset slice/canvas DEMOTED to the settings-popover bottoms (walks back his persist spec). Speed/duration placement confirmed good. Keyboard: he blessed space/delete/arrows, asked for a proposed standard set (filed in BACKLOG). Frame interpolation: still NOT implemented (25% = frame-hold via playbackRate; stutter is real duplication) — told him, backlog item stands.

**BUILD 246 SHIPPED the fit/fill toggle (source panel top-right corner, rides the component's getFit/setFit + a fill branch in arrangeSlots) — ARC 2 IS COMPLETE.** Builds 242–246 all await Daniel's verify (lists in CHANGELOG). **Next up per the arc sequence: ARC 4 (realtime perform — ghost/echo + follow ramp), which should start as its own session/conversation with the design questions from the plan (perceptual vs literal ramp, canvas-edits-while-live).**

**BUILD 236 (test pattern FIXED for the window destination):** the "test pattern doesn't work (iPad + desktop)" report was the known window-ignores-the-bus-pattern item — the pattern now rides the state channel and the window renders it on a 2D overlay (works sourceless). Both test-pattern triage items closed pending verify. **NEW DESIGN DIRECTIONS from Daniel filed in BACKLOG (Global control-area + Sources):** controls default to DROPDOWN menus (bands get superseded as touched); source info lives ONLY under the source panel (+ duration for motion data; drop the top-left caption AND the output-band resolution echo); sources are UNIVERSAL across modes (still mode gets a no-autoplay mini scrubber for video; live camera becomes valid in motion with Arc 4); **form-picker location is an OPEN DECISION to align on before Arc 2** (Claude leans: header of the form-settings stack).

**BUILD 235 (Arc 1 — record/pause icon+text component):** shutter moved right of the camera dropdown; new `.ot-icontext` pattern (12px glyph + label; glyph carries state color, text stays neutral) — red dot + "record" frozen, white pause bars + "pause" live; Lab button matrix gained the specimen row. First standard icon+text button per Daniel's spec.

**BUILD 241 (ARC 2c core — frame scrubber + dedup, needs verify):** video in still mode parks paused (no autoplay; Blink/Gecko never-played-paint gotcha handled with a play-nudge-park) with a mini frame scrubber under the source (latest-wins seeks); motion exit stays parked; the top-left caption no longer echoes source info (meta under the source is the one home, now with duration for video). **2c remaining: fit/fill toggle (source-overlay component work). Then 2d aspect expansion.**

**BUILD 240 (2b feedback):** stage-divider drag now shows a gray SKELETON tracking the output's landing size (canvas display:none during drag so the stand-in takes its spot); **BELOW is the default stack placement** per Daniel's lean (controls flow horizontally + wrap under each panel; `?panelctl=flank` keeps the columns for the A/B). Canvas reset CONFIRMED by Daniel.

**BUILD 239 (ARC 2b SHIPPED — control stacks, needs Daniel's comparison + walk-through):** the right panel + its divider are deleted; form settings (form-picker header + slice controls) stack with the source panel, canvas settings (+ NEW reset canvas) with the output panel. Placement comparison: default flank vs **`?panelctl=below`** — Daniel picks, we delete the loser (popover variant on request; pairs with the deferred dropdown direction). All ids preserved → wiring untouched. **Arc 2 remaining:** 2c source extras (fit/fill, mini scrubber, top-left caption dedup) → 2d aspect expansion.

**ARC 2a VALIDATED (Daniel, desktop + iPad) — renders and functions as expected.** One note fixed in **238**: the stage divider's touch target was only the 6px bar → the invisible ±12px `::after` hit extender (the panel divider's own pattern) makes the whole ~30px zone draggable, no visual change. **Now building: 2b (control stacks + placement variations).**

**BUILD 237 (ARC 2a SHIPPED — symmetric sibling panels, needs Daniel's full walk-through):** the main/side slot model is gone — source and output are peer panels in `#stageSplit` (middle-aligned, stage divider drags `session.stageSrcPct`, output-dominant 32% default, swap = row-reverse mirror). Mini-canvas 2D copy deleted; right panel is controls-only; `#sourceMeta` lives under the source panel. Third-panel-ready for output-staged (Arc 5). **Arc 2 remaining:** 2b control stacks w/ form-picker header + in-app placement variations → 2c source extras (fit/fill, mini scrubber, caption dedup) → 2d aspect expansion. (The parked mobile-blackout bug is in triage with the full what-we-tried trail; Daniel's go on Arc 2 + output-dominant default came 2026-07-06.)

**DANIEL'S SECOND VALIDATION (2026-07-06 night): ELECTRON PAUSE-ON-BLUR VERIFIED ON THE RIG** (fresh DMG, Syphon keeps playing while Arena is active — the Arc 0 headline fix works). PWA resolved (Vercel-side; deleted + rebuilt the domain link, deployed fresh). Form-picker placement AGREED (header of the form-settings stack). Dropdown-menu migrations DEFERRED by Daniel (not trivial, don't lose steam on the arc sequence). New edge bug filed in triage (timeline stale after returning to motion from a CAMERA still — suspect the capture path skips the motion rebind that loadImage runs). **ARC 1 is effectively CLOSED** — mode discipline (231), mobile pause (232), camera module (233/235) shipped; the bar-geography + save-menu-reorg remnants are deferred with the dropdown direction. **NEXT: ARC 2 (panel-sibling overhaul)** — increment proposal below/in chat, awaiting Daniel's go.

**DANIEL'S FIRST VALIDATION (2026-07-06 evening):** Electron pause-on-blur untested-on-rig yet; **keyframe undo PASSES**; **mobile pause toggle CONFIRMED GREAT**; **mobile save blackout NOT fixed by 230** → root-caused again + fixed in **234** (getExtension returns null on a lost context in Safari — extension now cached while alive); live-cam-case both-panels-reset = iOS page discard → the source-persistence backlog item. **Test pattern broken on DESKTOP too (Brave+Safari), not just iPad** → root cause = the known "window ignores the bus test pattern" item → FIXED in 236 (pattern rides the state channel; window renders it on a 2D overlay). **PWA stuck at 227** = Vercel deploy interference from the domain rename (fold.curiousimagery.com); code-side SW config is correct (`autoUpdate`) — check the Vercel deployments dashboard. **`npx electron-builder` at repo root FAILS by design** (no electron there; the deprecation warns were npm chatter from the ad-hoc download, unrelated to the Syphon leak) — the real command is `cd electron && npm run dist`, which I ran clean: `Fold Live-0.10.51-arm64.dmg` with the vendored leak-patched syphon.node applied.

**BUILD 233 (Arc 1 — desktop camera module rework, needs camera verify):** the camera dropdown is now the camera identity while in camera (all cameras + "quit camera" at bottom, replacing stop); the shutter is a record/pause toggle with a new `env.live.frozen` state — pause freezes the editable still but stays in the camera workflow (red record resumes the preferred device; picking a camera while frozen resumes on it; quit-while-frozen keeps the still). **Verify (Daniel):** the full flow in Build 233's changelog entry — camera paths are code-clean but unverifiable without a real browser. **Arc 1 remaining:** app-bar restructure per the spec (mode-selector position, filename under upload, camera name under the dropdown — pair on-screen), save-menu reorg.

**BUILD 232 (Arc 1 — mobile capture record/pause toggle):** the mobile freeze glyph is now PAUSE bars (was the stop square; fallback comment kept), titles follow; record/pause is the explicit toggle vocabulary, desktop adopts the same pattern in the camera rework. **Verify (Daniel):** on-device glyph check.

**BUILD 231 (Arc 1, increment 1 — Still|Motion mode selector + gating, needs A/B):** the motion toggle became a **still|motion segmented selector** (radio semantics; `#motionBtn` id preserved, new `#stillBtn`, `.ot-mode` styles); `updateMotionUI` now gates **save = still-only, output = motion-only** with two deliberate exceptions (live camera — motion rejects live sources until perform mode, so strict gating would strand live broadcast; running bus — stop stays reachable). Bar geography untouched; the app-bar relocation (mode selector position, camera dropdown, filename under upload) is the NEXT slice, to be paired with Daniel on-screen. **Verify (Daniel):** segments read as one control; save/output swap with mode; camera + mid-broadcast exceptions hold. **Arc 1 remaining:** app-bar restructure per his spec + camera-module dropdown/record-pause rework + save-menu reorg + mobile stop→pause.

**BUILD 230 (Arc 0, increment 3 — mobile save blackout FIXED, needs on-device verify): ARC 0 CODE-COMPLETE.** Root cause was self-inflicted: mobile's `pagehide` guard releases the GL context on every iOS handoff (saving included) and nothing restored it. Engine gained `reinitGL()` (reference-stable rebuild on the same restored context); mobile chrome wires `webglcontextlost` preventDefault + `webglcontextrestored` → reinit + resume, and `onVisible` requests `restoreContext()` when returning to a lost context. **Verify (Daniel, on device):** save → return → output intact (still, live camera, installed PWA). **All three Arc 0 bugs are now fixed pending verification; next: Arc 1 (mode discipline + global app bar).**

**BUILD 229 (Arc 0, increment 2 — keyframe undo INTEGRATED, needs verify):** history entries now carry a keyframe slice of `motion` (keyframes/selected/playhead) beside state; pushes added on keyframe add (not the automatic kf0 seed), delete, anchor toggle, retime drag, motion-JSON load; the four undo/redo triggers share one restore path that stops playback + rebuilds the timeline. Duration/loop/smoothing deliberately outside history. **Verify (Daniel):** undo/redo through add/drag/delete keyframe sequences interleaved with slice edits; fresh motion entry adds no odd undo step. **Arc 0 remaining:** mobile save-loses-output (WebGL context loss/restore).

**BUILD 228 (Arc 0, increment 1 — Electron pause-on-blur FIXED, needs rig verify):** selecting away to Arena paused Fold's playback (Chromium background throttling starves rAF/timers on blur, stops them entirely under occlusion). Fixed in [electron/main.js](../electron/main.js): `backgroundThrottling: false` in `webPreferences` + the process-wide `disable-renderer-backgrounding` switch. Electron-shell only; web build untouched. **Verify (Daniel):** `npm run build`, `cd electron && npm start`, broadcast with motion playing → focus Arena windowed AND fullscreen → playback + live frames continue. **Arc 0 remaining:** keyframe-undo integration; mobile save-loses-output (WebGL context loss/restore).

**BUILD 227 (Lab token auto-discovery + docs hygiene):** the Lab's token catalog is no longer hand-listed — new [src/lab-tokens.js](../src/lab-tokens.js) (`discoverTokens`/`groupTokens`/`TOKEN_GROUPS`) parses every `--name:` straight from tokens.css and buckets it; [lab.js](../src/lab.js) renders the groups. **Add a token → it appears in the Lab on its own with a live value + usage count.** A new CATEGORY needs one `TOKEN_GROUPS` rule; unclaimed tokens land in a new **"unfiled"** drift section. Node-validated (85 tokens, all placed, 0 unfiled; now also shows the 6 control/state tokens that were hidden). Icons + form thumbnails already auto-enumerate; components/text-styles stay hand-authored (intended states can't be inferred). Fixed the stale CLI line re: the DMG icon. **Docs (non-code):** `BACKLOG.md` trimmed 571→~320 (shipped/historical narrative → pointers); new **`ARCHIVE.md`** holds the reasoning-rich history verbatim (mobile-UX exploration, node-syphon saga, diagnostics spec, strategic analysis); byte-exact pre-trim backlog at git `4046013`. `DESIGN.md` gained the cross-shell "extending onto new surfaces" contract + an asset-handoff-mode rule (ask: redraw-intent vs literal). **Verify (Daniel):** `/lab.html` token sections look as before + a new "Control / state" section; add `--c-test:#f0f;` to tokens.css → it auto-appears under accents (0×), then remove.

**BUILDS 224-226 (Daniel's testing fixes + cursor loop) — cursors CONFIRMED GOOD:** **224** fixed the radial corner-join regression (closePath only when the chain returns to start — radial's outerEdges is an OPEN arc), stop icon 11→16px, and the selfie-camera bug (freeze→go-live reverted to rear; now `lastFacing` remembered). **225/226 cursor loop:** 225 wrongly embedded Daniel's RAW SVG (oversized/unnormalized/180°-off); **226** instead DRAWS the design (rotation arc + tangent arrowheads) in our stroked style at 32px, weights shared with the scale double-arrow, arc bulging left toward the +X centre. **Daniel confirmed on screen: rotate cursors feel good ("like they've always been like this").** **Still verify:** segment corner-join across all closed forms + mobile + motion-render source preview (Build 223/224).

**BUILDS 222-223 (Daniel's review w/ assets):** **222** camera-flip icon = Daniel's SVG normalized to the 24-grid; mobile UX: source tab always "+", capture/freeze = new `stop` icon (aperture `captureCam` now unused, Lab 0×). **223** segment-overlay corner-join — `strokeEdges` draws the boundary as one closed path w/ `lineJoin:round` (ISOLATED + revertible; **verify everywhere: desktop/mobile/motion-render source preview, all polygon forms**). **DMG icon: replace `electron/build/icon.png` (or drop an `.icns` → set `mac.icon: build/icon.icns`; .icns is Apple Icon Composer's output, supports the appearance/monochrome variants).** **STILL OPEN — CURSORS (deferred deliberately):** Daniel supplied rotate-cursor SVGs (his white-fill style, better arrowheads) + wants custom white-fill SCALE cursors to replace the OS resize cursors (consistency > direction; he prefers our white-fill). NOT done blind — cursors are 100% invisible to me, and his filled-shape needs hotspot/scale/rotation calibration → MUST be a paired/verify loop (he glances at a screenshot per attempt). His assets are in the thread.

**BUILDS 220-221 (Daniel away, both tracks back-to-back; ALL perceptible/visual — he A/Bs on return; fallbacks preserved):** **220 Track (a) consolidation:** base button radius 4→6 (desktop buttons unified); `.ot-btn` translucency+blur removed → solid (vestigial since the bar went opaque); mobile `.m-control-row` 13→12. **221 Track (b) assets:** real SVG undo/redo/swap icons replacing unicode `← → ⇄` (icons.js + index.html inline + `.ot-btn svg`; unicode kept as fallback comments); keyframe pin → triangular notch flush on the square (filled=locked per Daniel, hollow=auto; old diamond as fallback comment). **BLIND ATTEMPTS — I can't see them; revert is one line.** **DEFERRED (need a visual loop, can't do sub-pixel geometry blind):** camera-flip arc/arrowhead gap + rotate-cursor arrowhead direction → Daniel sends SVGs or we pair. **DMG icon confirmed:** replace `electron/build/icon.png` (1024px) to customize. **Daniel prefers SVG over screenshot for asset hand-off** (plug-in-ready).

**CLEANUP BATCH 1 (Build 219):** Droste offset diamond → white (was off-token #aadcff; PERCEPTIBLE — A/B); orphan `--c-red-500` removed (0×); sliders icon #121212 knobs → currentColor (PERCEPTIBLE — A/B); favicon home (`public/favicon.svg` + link); DMG-icon pipeline (`electron/build/icon.png` placeholder from fold-icon.svg + electron-builder `mac.icon` + README — **UNTESTED, verify with `npm run dist`**); Lab App-icons split into PWA/favicon/DMG homes. **Daniel wants me LESS conservative: proactively merge pixel-drift + perceptible color/text consolidations, and FLAG the visually-impactful ones for his A/B validation.** **Open visual items Daniel raised (in BACKLOG cleanup inventory):** rotate-cursor arrowhead direction is hard to read + cursor styles should MATCH the scale cursors (custom-SVG-white-fill vs OS resize cursors); keyframe pin should be a triangle-notch flush on the square top (+ rethink locked/auto fill); camera-flip icon needs a gap between arc & arrowhead; swap/undo/redo unicode → real SVG icons; **captureCam UX (Daniel's call): source tab always "+", replace aperture capture icon with a STOP icon (red dot for go-live reads great)**; mobile target icon (settings↔source) unintuitive. **Button-variant analysis:** intent colors + the bigger touch heights are defensible; the `.ot-btn` translucency/blur is VESTIGIAL (bar is opaque now) + radii 4/6/8 drift → collapse.

**ARC STATUS (Build 218):** the Lab inventory is essentially complete — tokens w/ working usage counters, named text styles `.t-*` (proposed), icons+glyphs, cursors, affordances (incl. Droste diamond), components (button state-matrix + menus), building blocks, composites (timeline/clip/proper desktop modal), cross-device states, CLI cheat sheet. **A consolidated `DESIGN-SYSTEM CLEANUP INVENTORY` is in BACKLOG** (icons / cursors-affordances / buttons / text / states / modals / spacing-radii / assets). **What's LEFT in the broader design-system effort:** (a) the CLEANUP consolidations (collapse the diffs the Lab revealed — named-text migration, button-emphasis disambiguation, empty-state unify, off-token reconcile, corner-join, icon fixes); (b) **spacing-token adoption** (deferred from the foundation arc — `--space-*` read 0×); (c) the **iPad-landscape padding hack** (deferred, needs live inspection); (d) **THE CAPSTONE Daniel flagged: rework the global nav / app-bar to actually CONSUME the system + Lab** — the first real "use the design system" surface, the logical next step now the Lab is stable. **Affordance SVGs: Daniel will author his own; just integrate when received — do NOT rework the procedural-canvas generation (breaking-change risk).**

**LAB NOW (Build 217):** 17 sections — tokens (color/type/radii/spacing w/ working usage counters), **functional text-styles** (composed roles: heading/label/value/caption/status/meta/mono — surfaces text-style over-variation), icons (+button glyphs), cursors, affordances (scale/rotate/center/Droste-diamond), components (button state matrix + menus), building blocks (grippy/LED/separators), composites (timeline/clip/modals), cross-device states, + a CLI cheat sheet. **Candidate next consolidations the Lab now makes obvious:** a small NAMED text-style set; collapse the 6 button-emphasis treatments; reconcile off-token colors (#aadcff diamond); unify the 3+ empty messages. **Still open (BACKLOG):** SVG-extractable affordances, slice corner-join polish, the favicon/DMG-icon assets.

**USAGE COUNTER FIXED + VALIDATED (Build 216):** the prior CSSOM-based attempts (211, 214) silently returned nothing; replaced with a pure-string parser (`parseUsage` in lab.js, CSS imported via `?raw`), **validated in Node** (73 tokens w/ consumers; `--text-2xs`→`.mf-time`; `--space-*`/`--focus-ring` 0×). LESSON: I can't run a browser, so browser-only code must be validated by extracting the logic and running it in Node (or it ships blind — happened twice here). **STILL OPEN:** SVG-extractable affordances; the slice corner-join polish; empty-state + off-token-diamond consolidations (all in BACKLOG).

**LAB GALLERY EXPANSION (Build 215, Daniel-prioritized):** added Building blocks (grippy handles desktop/mobile, traffic-light LED states, separators), Composites (timeline + keyframe marker states + playhead; clip-editor range with region/handles/blue-cut/playhead; modal cards desktop↔mobile), and Cross-device states (empty output + source, desktop vs mobile — surfaced that desktop has THREE different empty messages). **STILL OPEN:** (a) the **usage counter reads 0× in Daniel's browsers** — Build 214's `?inline`+CSSOM fix did NOT work; being redone (Build 216) as a CSSOM-free string parser validated in Node (I cannot run a browser, so prior "fixes" shipped unvalidated — root cause is the browser CSSOM `.sheet.cssRules` path). (b) **SVG-extractable affordances** (Daniel wants editable starting points; they're procedural canvas today — render faithful SVG specimens). (c) logged: slice outline **corner-join** roughness (`strokeEdges` draws per-edge subpaths, no corner join — set `lineJoin` + draw the outline as one path).

**LAB REVIEW ROUND 2 (Daniel, Build 214):** **usage counter now actually works** — `document.styleSheets` introspection was returning nothing, so it now imports the CSS as raw text (Vite `?inline`) and parses it via a `<style>` it owns (reliable in dev/http/file://). **CLI cheat sheet** got a detailed second line per command (when to run / deps / output). **Affordances:** added the diagonal corner scale arrow + the **Droste blue-diamond offset handle** (from `droste.js drawOverlay`, `rgba(170,220,255)`) — surfaced that its blue `#aadcff` is OFF-TOKEN (≠ `--info #4aa3ff`). **DMG icon confirmed = default Electron icon** (no `.icns` in electron/). **NEXT PASS (Daniel prioritized): the component-gallery EXPANSION** — base blocks (grippy/HR/traffic-light LED), composites (timeline w/ keyframe states + scrubber, clip-editor range slider + slice point, modals w/ blurred backdrops, composite menu-with-icons), and cross-device empty-state comparisons (output + source canvas, desktop vs mobile). PLUS make the affordances **extractable as SVG** (Daniel wants editable starting points to author replacements; they're procedural canvas today — render faithful SVG specimens). See BACKLOG "Lab review findings" + "Component-gallery expansion".

**LAB REVIEW (Daniel, Build 213) — fixes done + findings logged.** Daniel reviewed the Lab in prod/Brave. **Key gotcha:** he was on `file://` (the usage cross-ref needs the http dev server — a banner now explains this), and his `npm run dev` didn't start because my earlier dev server still holds port 5173 (it IS running at http://localhost:5173/lab.html). **Lab bugs fixed (Build 213):** imported `mobile/styles.css` (mobile specimens were unstyled), fixed the usage `[hidden]`/badge-stretch, app-icon overflow, illegible affordances (now drawn in slice context), + a CLI cheat-sheet modal (copy-to-clipboard, in the nav footer). **CORRECTED a wrong assumption:** `afScaleArrow`/`afRotationArc` are LIVE (8 overlay.js call sites), not cruft. **App-code touched:** `mobile/icons.js` — deleted orphan `aperture`; normalized `captureCam` viewBox (renders identically — NEEDS Daniel's mobile verify). **Findings logged to BACKLOG (see "Lab review findings"):** no favicon (+16px legibility), DMG≠PWA icon, undo/redo/swap want real icons, and the component-gallery expansion (base blocks grippy/HR/LED next; composites timeline/clip-scrubber/modals + cross-device empty states need a measured pass).

**"LAB AS VISUAL INVENTORY + USAGE INSTRUMENT" ARC — IN PROGRESS.** Goal (Daniel): make the Lab the tool that reveals over-variation (near-same buttons / pixel increments / modal styles), problem areas (sloppy iconography, ambiguous button states), and functional gaps (overflow pattern), from a cohesive base we can then get more expressive from while staying bare-bones. **Consumers:** the app-bar refine pass, motion-control IxD, the segment-manipulation affordances/cursors, and perform mode all build on it. **4-increment plan:** 1 ✅ Lab section-nav + icons inventory (Build 209) → 2 ✅ cursors + affordances inventory (Build 210) → 3 ✅ usage cross-reference (Build 211 — live `document.styleSheets` introspection; each token shows `n×` consumers on click, `0×` = unused/cut-candidate; CSS half automatic, JS-icon half = grepped ICON_USAGE map, no build step) → 4 ✅ component gallery as a STATE MATRIX (Build 212 — button families × class-states side by side; surfaces SIX near-identical emphasis treatments = the "loop-on reads like primary" ambiguity; desktop↔mobile menu pairing as a divergence detector). **ARC COMPLETE.** The Lab is now a visual inventory (icons/cursors/affordances/components) + a usage instrument (live token cross-reference). **NEXT (supervised, visual-change passes the Lab now de-risks):** icon cleanup (kill orphan `aperture`, de-`#121212` the fills, restyle `captureCam` to the 24×24 stroke set, grow the set to the desktop bar), affordance/cursor redraw (the lost Droste rotation handle, crosshair), button-state disambiguation (collapse the 6 emphasis treatments), menu unification (the two divergent menus), spacing-token adoption (the `--space-*` 0× tokens), then the app-bar refine pass. **Inc 2 note:** exported `afScaleArrow`/`afRotationArc` from `shell/overlay.js` (2 keywords, zero behavior change) so the Lab renders the REAL affordance primitives; the fuller touch composite (move/segment/square/droste-arm in `drawTouchAffordances`) needs a geometry harness — follow-on. **Increment 1 findings (auto-surfaced by the Lab):** `aperture` orphan/unused; `sliders`/`record` bake `#121212`; `captureCam` uses a Material viewBox (different weight); ALL ICONS are mobile-only (desktop bar is text → the icon/overflow gap). **This arc was prioritized OVER the app-bar composition pass and the iPad padding bug** (Daniel's call). The base-8 audit (radii off-grid 1/3/6/10 with 6px the button radius; spacing off-grid 6/10 heavy) feeds the "reduce in-between states" goal — normalize per-surface, not globally.

**DESIGN-SYSTEM + UI LAB ARC — IN PROGRESS (plan: `~/.claude/plans/in-a-previous-session-rustling-chipmunk.md`).** UI infrastructure to de-risk future polish (not a redesign); bottom-up, GLOBAL-first, strict visual parity except the deliberate slider/scrub touch-target convergence. **Increment 1 ✅ (Build 205):** new `src/shell/tokens.css` (PRIMITIVES `--c-*` faithful to every observed value + SEMANTIC aliases `--bg`/`--surface`/`--text-dim`/`--accent`/`--ok`/`--danger`/`--fill-bright`/… that CSS consumes + coarse-pointer `--touch-target` override) linked in index.html before styles.css (reaches desktop + mobile via the shared `<head>`); new UI Lab (`lab.html`/`src/lab.js`, third Vite entry) renders all tokens live (`getComputedStyle`) + core controls, at `/lab.html`. **Increment 2 ✅ (Build 206):** `styles.css` now CONSUMES the tokens (all color/font-size/border-radius/disabled-opacity literals → tokens). **Parity proven mechanically** by a resolve-and-diff harness (expand every `var()` back to its literal, diff vs pre-change file): 0 non-cosmetic deltas + 1 intended delta (body `font-family` → `--font-sans`, the conscious sans-stack unification, invisible on Apple/Blink). Left literal by design: `rgba()` alpha colors, layout dims, transition durations, spacing (the `--space-*` scale exists but isn't consumed yet — a low-risk follow-up). Daniel smoke-tested: desktop visually unchanged ✓, Lab good ✓. **Increment 3 ✅ (Build 207):** `mobile/styles.css` adopts the tokens; `--m-bg/fg/dim/line` folded into the shared semantics (`--m-tabbar`/`--m-divider` layout vars stay local). **The 4 surfaced inconsistencies are now RESOLVED (Daniel-approved):** (1) record red unified to `--danger` (#ff5a5a); (2) sans stacks unified in `--font-sans` (both surfaces); (3) mobile canvas backdrop kept distinct/darker (tokenized, `--c-black`/`--bg`); (4) near-duplicate darks light-collapsed (`#161616→#141414`, `#1c1c1c→#1a1a1a`; dropped `--c-neutral-825/-775`). Desktop deltas from the collapse verified by the harness = only the 3 collapse spots + the font line. Daniel confirmed mobile smoke test looks good. **Increment 4 ✅ (Build 208):** slider/scrub/button touch targets converge onto `--touch-target` (44px coarse / 32px fine). Removed mobile's conflicting `.m-control input[type=range] { height:28px }` override → mobile sliders inherit the shared coarse rules and are now a regular **44px** (was a cramped 28px — the one intended visual change of the arc). `--touch-target` also drives the coarse hit areas of `.scrub`/`.ot-btn`/`.form-thumb`/`.mf-btn` (pure parity, now centrally editable). Desktop parity re-proven (only the Inc-3 collapse + font deltas; touch-target tokenization moved nothing on desktop). **AWAITING DANIEL'S ON-DEVICE FEEL of the mobile slider touch target.** **Increment 5 ✅ (docs-only):** `docs/DESIGN.md` written (two-tier model + full token map + the working loop + responsive/touch-target rules + the Lab); `ARCHITECTURE.md` updated (design-system layer in the dir map + layering vocabulary). **THE FOUNDATION ARC IS DONE.** Two clean follow-ups remain (NOT blockers, in BACKLOG): (a) tokenize spacing (the `--space-*` scale exists, stylesheets still use literal padding/gap; leave layout-coupled values like `.ms-stage` 24px literal); (b) the iPad-landscape `34px` top-padding hack (behavioral, needs LIVE inspection — do not blind-guess). **The design system is now the home for future polish:** the deferred Build-178 app-bar IxD batch, motion-controls IxD, and touch-target-scaling all build on it. **Verification harness for any future token adoption:** a resolve-and-diff approach (resolve every `var()` back to its literal, diff vs a pre-change copy) proves parity mechanically; it's how Builds 206-208 were verified. **Pending Daniel checks across the arc:** desktop parity (done ✓), mobile parity (done ✓), the mobile slider touch-target on-device feel (Build 208), and a play with editing `tokens.css` to feel the "edit once" leverage.

Daniel's two smoke-test observations (both NOT from this work, noted for later): mobile sliders intermittently showing the right darker style (HMR/SW caching during test; parity-proven unchanged) and the PWA tab bar intermittently adding extra bottom margin (`env(safe-area-inset-bottom)` varying as Safari's toolbar shows/hides — inherent to honoring insets verbatim; candidate for a separate look).

Recent: **output-bus readback refactor — hidden second engine + getImageData (199), pause-flicker fix (200), edit-lock during playback (201), graceful second-GL-context failure (202), camera-switch aspect-stretch fix + DMG named with the real app version (203), upload persists during live camera (204)**. **DMG verified. Output window 120 fps Chromium.** **One-command DMG build:** `cd electron && npm run dist` (names it `Fold Live-<version>-arm64.dmg` from `src/version.js`).

**HARDENING PASS (before the next arc) — going well.** **Daniel's results (Build 202 DMG):** test pattern orientation in Arena ✓ clean; live camera ✓; **~15 min soak across video + camera + stills → steady under ~2GB total (Electron + helpers), no growth** ✓ (the per-frame getImageData churn is NOT a leak). **Bugs found + fixed (203):** (a) **camera-switch aspect stretch** — switching to a non-16:9 / renegotiating camera (iPhone Continuity, Dell) stretched the broadcast vertically while the preview stayed correct; the hidden engine kept the prior camera's `sourceAspect` because `camera.js` reuses one `<video>` element across switches (reference unchanged) — fixed by re-`setSource` on a DIMENSION change in `output-engine.js syncSource`. (b) **DMG mis-named** `0.1.0` (electron shell's package version, not the app's) — new `electron/scripts/build-dmg.cjs` reads `src/version.js` and names it correctly; `cd electron && npm run dist` is the one-command build. **UX (built, 204):** during live camera the only exit was "stop camera," which cleared the source and so stopped the broadcast. The upload button now PERSISTS through live camera (Daniel chose leftmost full button), so you can switch to a still/video without dropping output (the upload control accepts both; loadImage/loadVideo keepSource so the broadcast survives). **Remaining regression spot-checks (Daniel):** a still export, a video export, web record.

**READBACK REFACTOR (199) — VERIFIED, BIG WIN (Daniel smoke-tested in Electron):** 4K ~35-40fps, HD 100+fps (was 4K ~5 / HD ~22), and record is less compressed + much higher-fps. The hidden second engine + getImageData fast path delivered. **One bug found + FIXED (200):** pausing motion playback on a long clip (6-10 min) flickered the Syphon broadcast through stray clip timestamps before settling — the bus's continuous loop was uploading the decoder's intermediate frames during the pause-seek (`loadPlayheadIntoState` → `scrubVideo` seeks to the playhead). Fixed by gating the output engine's per-frame video re-upload on `!env.sourceVideo.seeking` (hold the last settled frame, like the preview). **Daniel confirms 200 fixed it (can't repro).** **Second bug found + FIXED (201):** dragging the slice (or pinch/twist on the output) during playback flickered the manipulation into the Syphon broadcast — the main app never explicitly locked param edits during playback (the playback tick just clobbers them before the preview paints), but the drag writes `state` transiently and the bus renders `state` on its own loop, catching the uncommitted edit. Fixed with a shared `isMotionDriven()` lock (`main.js`) threaded as `editLocked` into the source overlay (`overlay.js` onDown/onWheel) AND the output gestures (`output-gestures.js`) — both go read-only while playback/scrub drives the state. Makes the restriction real instead of clobber-based. **Daniel confirms 201 works.** **Muddy/torn thumbnails — REPRO'd + BACKLOGGED (moderate-low):** Daniel pinned it down — it's the **filmstrip/scrub THUMBNAILS** (not the main output preview, not the refactor): rapid keyframe+scrub on a long clip captures cold-decoder partial frames in the seek-per-cell filmstrip build (`buildFilmstripVideo`), self-resolving as the decoder warms. No clean no-tradeoff fix (rVFC is unsafe on the occluded source video; a post-seek delay is a latency band-aid). Documented with levers in BACKLOG ("Filmstrip/scrub thumbnails go muddy…"); best lever later = decoder warming on load. **Video-source blackout on swap (motion mode) FIXED (197):** swap re-mounted a blank video canvas that only scrub/playback repainted — `arrangeSlots` now repaints + rebuilds the filmstrip; Daniel confirms it holds (can't repro). **Main-wrap stage-fill (198):** made the non-swapped main wrap `width/height:100%` (was `max-height:100%`) to match the swapped branch. After Daniel's retest this **FIXED Safari not honoring the wide aspect** (good), but **did NOT fix the vertical top-float** — Daniel now sees the 16:9 preview pin to the top uniformly across Safari + Firefox + Brave + Electron. So top-float ≠ the `max-height`/`height` thing; the static chain (`.ms-stage` flex-center → 100%×100% wrap → canvas; `.output-status-area` is `position:absolute`) should center, so the cause needs **live computed-style inspection**, not more static reading. LOW priority per Daniel. Don't ship another blind guess.

**PERF WORK — REFACTOR SHIPPED + VERIFIED (Build 199; flicker follow-up Build 200).** Daniel's smoke test: **4K ~35-40fps, HD 100+fps** (from 4K ~5 / HD ~22), record less compressed + faster. The benchmark (Build 196/197, Electron, M5 Max, ANGLE-Metal, 1080p) had settled it: **A `readPixels` = 42.9ms vs B `drawImage GL→2D + getImageData` = 4.7ms → ~9.1× FASTER**. Orientation across the native bridge came through clean (no upside-down report). **What shipped:** the output bus renders through a **hidden second engine** ([shell/output-engine.js](../src/shell/output-engine.js) `createOutputEngine` — offscreen `<canvas>` + lazy `createEngine` + 2D capture canvas; the EXACT pattern the output window ships at 120fps), and [shell/fold-adapter.js](../src/shell/fold-adapter.js) `renderFrameAt` delegates to it instead of `engine.exportFrameRaw`. **Source-sync** shares the SAME source element the preview uses (`env.engine.getSourceImage()`): re-`setSource` on reference change, `updateSourceFrame` per frame for camera/video — no second camera (in-document, unlike the popup). **Orientation flipped bottom-up → top-down:** the Frame carries a `topDown` flag (+ optional `canvas` passthrough) documented in [engine-adapter.js](../src/stage/engine-adapter.js); [recorder.js](../src/stage/recorder.js) drawImages the capture canvas straight in (no per-row flip, no putImageData — also speeds web record), [syphon-sink.js](../src/stage/syphon-sink.js) passes `frame.topDown` to the host, and [electron/preload.js](../electron/preload.js) + [electron/syphon-bridge.js](../electron/syphon-bridge.js) thread a `flipped` arg so Syphon now publishes `flipped:true` (was hardcoded `false`). [test-pattern.js](../src/stage/test-pattern.js) emits top-down too (stays a faithful probe). Stills/video-export + the engine FBO path untouched (`exportFrameRaw` stays for the diag benchmark, off the hot path). **Expected:** Syphon/record HD from ~22fps toward render-bound (~100+fps), 720p no longer the ceiling. **VERIFY FIRST (the one visible failure mode — orientation flip across the native bridge):** arm the **test pattern** on the bus → in Arena "TL" reads top-left, "▲ TOP" points up, circle round, red border whole. If flipped/mirrored, the bridge `flipped` arg (or the sink's `topDown`) is the fix. Then: broadcast a live 1080p kaleidoscope, confirm the diag op-ring `read` drops ~44ms → ~5ms and fps climbs; record a take (upright, high-fps). **This does NOT remove the ultimate IOSurface/native zero-readback ceiling — it closes most of the gap with pure web tech.** Deferred micro-opt: skip getImageData entirely when only the recorder is armed (kept the adapter sink-agnostic for now).

**KEY FINDINGS from Daniel's DMG/cross-browser testing (2026-06-18):**
- **macOS gives browsers NO multi-touch** (`?inputdebug`: Movink + Sidecar both = single `mouse` pointer, `touches=0`). iPad-style gesture is impossible on a Mac touchscreen — see memory `reference_macos_no_multitouch_in_browsers`. **Consequence (Daniel): gestural perform mode on the Mac/Syphon rig is NOT feasible via Movink/Sidecar/trackpad; perform-mode input must lean on MIDI (APC40) + game-controller (Gamepad API) mapping** — rotary→angle, slider→scale, joystick/XY→position. Elevated for the perform-mode arc; both are web/Electron-native (no native module — confirm Web MIDI in Electron with a quick spike). Trackpad-pinch→scale (wheel+ctrl; Safari adds rotate via gesture events) is a possible desktop convenience, scale-only on Chromium.
- **Syphon output fps (Daniel, DMG + Electron):** motion playback 4K ~5 / HD ~20; live camera 1080p ~45; 720p ~43. **NOT a regression** (Build 181 measured ~11-12 motion / ~18-20 camera — current is better). The Syphon path is inherently CPU-readback + two-IPC-copy bound; levers are **zero-copy frame transfer** (BACKLOG, the next optimization) then the **IOSurface path** (native, zero readback). Check the diagnostics op-ring for the per-frame gl/readback/publish breakdown before optimizing.
- **Bug — panels black out on resize in motion mode while broadcasting** (Brave; related Firefox blackouts/timeline-frames-missing, undocumented). Build 193 adds context-loss detection to disambiguate context-loss vs repaint-bug. Awaiting Daniel's data points (see BACKLOG). **Recovery: exit/re-enter motion** — which suggests a repaint bug (a truly lost context wouldn't recover without GL re-init), but confirming.

**Open from Daniel's cross-browser test (Build 189–191, all in BACKLOG):** (1) **Safari: the output window's camera starves the main app** (Safari allows one camera consumer; the popup's own `getUserMedia` blacks out main Fold) — a real breakage, decision needed (lean: frame-push fallback for the camera source only). (2) **Firefox output window ~1 fps** (likely rAF throttling of the unfocused popup). (3) **Hybrid input:** `touch-action:none` shipped (Build 191) as the first gesture fix + a `?inputdebug` readout; awaiting Daniel's per-device results (Movink / Sidecar / desktop trackpad) to add the trackpad-gesture + remaining handlers. Chromium output window is unaffected by 1 & 2.

**ACTIVE ARC — Fold Live enablement + hardening cluster** (plan: `~/.claude/plans/picking-up-from-our-prancy-shamir.md`). A focused, ship-and-stop cluster between Phase 0 and perform mode. **All four increments built:** **1 ✅ camera source picker (Build 188)** → **2 ✅ output-window GPU-direct path (Build 189)** — the popup is now a SECOND engine view rendering on the GPU (`shell/output-window.js` + `output.html`/`src/output-view.js`, `BroadcastChannel` state sync, `needsBus:false`); replaced the CPU `stage/window-sink.js`. → **3 ✅ Movink rehearsal smoke test run** — surfaced the hybrid-input findings below (Phase 0 verify #8 effectively closed) → **4 ✅ local unsigned DMG (Build 192)** — `electron/` builds `Fold Live-0.1.0-arm64.dmg` via electron-builder (unsigned, arm64; `npm run dist`). **Spun OUT into its own later arc:** the app-bar IxD batch + the whole UI-authoring question → a **design-system layer + UI Lab** built in the vanilla-JS architecture (NOT React/Plasmic — decided this session; see BACKLOG). **After this arc:** perform mode (Phase 2 munging Phase 1 — perform core leads, motion-IxD cleanup rides along only as reused).

**Where to pick up next:** the cluster is built; remaining work is the open items from Daniel's testing (above), in this order: (a) **hybrid gesture input** — implement the precise handlers once Daniel reports the `?inputdebug` results per device; (b) **Safari output-window camera conflict** — frame-push fallback (Daniel chose: queue after the DMG); (c) **Firefox output-window ~1 fps**. Then the next ARC: design-system + UI Lab, or perform mode. **Verify pending (Daniel):** the DMG launch + packaged Syphon→Arena; the Build-191 gesture fix on the Movink. Deferred infra: a shareable/notarized/universal DMG needs the $99 Apple Developer Program (not enrolled; the arm64 binary is also why the DMG is arm64-only for now).

**Build 187:** output-window polish — scales past 100% to fill the display; resolution picker locks while outputting (like frame aspect). **Build 186 (Increment 6 — Phase 0 closed):** output row is a single-select **destination picker + start/stop** (stop to change, last selection retained); new **universal external-window sink** (`window.open` + canvas, web AND Electron — standalone output, no Arena). Destinations: external window everywhere, Syphon on the Electron host. Record is a separate concurrent control. **Build 185:** the node-syphon memory leak is FIXED (Daniel confirmed flat memory live in Arena). Root cause was their native `publishImageData` never releasing the per-frame Metal texture (upstream #45); ~5-line fix built into a vendored binary (`electron/vendor/node-syphon/syphon.node`) applied by a postinstall hook (`electron/scripts/patch-node-syphon.cjs`) over the npm-installed `node-syphon`. Clean to revert (bump node-syphon + delete the hook when upstream merges). Caveats: arm64-only binary (universal at packaging time); node-syphon is GPL-3.0+ (AGPL-compatible). **Next: PR the fix upstream** (#45; Claude drafts, Daniel reviews wording). **Build 184:** output-surface fixes from Daniel's Build-183 review — (a) the **test pattern runs with no source** (arm record/broadcast on the pattern alone for a pre-show pipe check; output button now reachable whenever a sink exists); (b) the **frame-aspect control locks while outputting** (dimmed + tooltip; it was silently inert because the bus locks resolution while running — set aspect before arming). Zoom/rotation/OOB stay live (they DO update in Arena). **Memory leak: option (b) periodic-recycle RULED OUT** — Daniel confirmed disarming halts growth but memory doesn't recover until quitting Electron, so node-syphon holds the leaked native memory until process exit. Realistic fix is the IOSurface path; decision pending (see below).

**Build 183:** shipped the output **test pattern** (a toggle in the output row → the bus publishes a known reference frame: corner labels + TOP arrow + border + circle + color bars + resolution; use it to verify orientation/mirroring/crop/aspect/color in Arena vs a recording). **And ROOT-CAUSED the memory leak:** a headless probe proved `node-syphon`'s native `publishImageData` leaks ~14.2 MB (one full frame) PER CALL — V8 metrics flat, `gc()`/`dispose()` reclaim nothing. It is upstream (the JS wrapper is pass-through; our IPC is fire-and-return; 1.5.0 is the latest version). The Build-182 backpressure fix was still right (it bounded the IPC queue); the residual climb is purely the native per-call leak. **Decision pending with Daniel on the fix direction** (see below + BACKLOG). **Build 182 (v0.10.0 MILESTONE):** "Fold" live in Arena + the backpressure fix (`ipcRenderer.send` → `invoke`/`handle` + in-flight flag; drop excess frames). **Crop — ROOT-CAUSED by Daniel:** Arena receives a square 1920² canvas and scales it to fill; changing Fold's canvas aspect mid-broadcast does NOT update Arena because the bus locks its resolution while running (by design — can't resize a live Syphon texture / recorder canvas). So set the aspect BEFORE arming. Daniel proposed disabling the canvas controls while a broadcast is live; see the open follow-up below (recommend: lock only the frame-aspect control, keep zoom/rotation/OOB live as performance controls). **fps (Daniel):** live camera ~18-20, stills/video/1080p motion ~11-12 (two-copy path; zero-copy backlogged). **Build 181/180/179:** Inc 5 code / Inc 4 shell / Syphon-name web-leak fix.

**node-syphon memory leak — FIXED + SHIPPED (Build 185, Arena-confirmed). Only the upstream PR remains.** Root cause: their native `MetalServer.mm` `publishImageData` never released the per-frame `MTLTexture` (their own commented-out TODO; the adjacent `PublishSurfaceHandle` does it right). Fixed in a sibling fork clone `/Users/danielnelson/Code/node-syphon` (local `texture` + `addCompletedHandler` release), built with node-gyp + Command Line Tools (no full Xcode; N-API → loads in Electron 42). Durable integration done: vendored binary `electron/vendor/node-syphon/syphon.node` + postinstall hook `electron/scripts/patch-node-syphon.cjs` re-applies it over the npm `node-syphon` after every install. Probe FLAT (~110MB vs 5,761MB); Daniel confirmed ~1GB sustained live in Arena. Revert when upstream merges = bump node-syphon + delete hook/vendor. arm64-only (universal at packaging); node-syphon GPL-3.0+ (AGPL-compatible). **PR opened: [node-syphon#46](https://github.com/benoitlahoz/node-syphon/pull/46)** (fork `curiousimagery/node-syphon`, Closes #45, awaiting maintainer review — GitHub notifies Daniel). When merged + released: revert our vendored patch (bump node-syphon + delete the postinstall hook + `electron/vendor/`). Full detail in BACKLOG. **Earlier (now historical):** Daniel's device test had RULED OUT option (b): disarm halts growth but memory holds until process exit, so recycling the server won't reclaim. Remaining options (BACKLOG "node-syphon publishImageData memory leak"): **(a) IOSurface-reuse** via `publishSurfaceHandle` + one reused surface — the architecturally-correct fix (also kills the copy/fps cost), but it's the first native code we'd OWN (a small addon to create/write an IOSurface), with build + code-signing implications; **(d) fork+patch node-syphon's native source** to release/reuse the texture — reuses their Metal plumbing, smaller code change, but means building node-syphon from source + maintaining a fork. Daniel leans (a) ("architecturally correct"), rejected (c) lower-res. **Next step (proposed): investigate node-syphon's GitHub source + open issues** to size (a) vs (d) concretely (is the native leak a small patch? is the maintainer active?) before committing to native work. **DONE Build 184:** the canvas frame-aspect lock during output, and test-pattern-needs-no-source.

## FOLD LIVE — Phase 0 in progress (live performance shell → Electron+Syphon → Arena)

**Active program.** Durable spec: `~/.claude/plans/in-our-last-thread-splendid-sparkle.md`. Working build plan for the current session: `~/.claude/plans/zany-baking-beaver.md` (the Phase 0 increment sequence). The **controls/IO/settings inventory + per-platform availability matrix + locked UI decisions** live in `docs/CONTROLS.md` (the coherent "sense of truth" for planned output UI — read it before touching the output surface). **Daniel's longer-term intent for that doc (deferred, not built):** grow it into a SYSTEM-WIDE reference that becomes the basis for a user manual and a systematically-built icon library. Big lift, a rabbit trail from the current build work, so it stays the lightweight inventory for now; revisit when there's appetite. Read those first if you're picking this up.

**Approach (locked with Daniel):** an engine-agnostic `src/stage/` layer (first tenant Fold) renders the program once to an FBO and fans it to many sinks (record-to-disk now; Syphon + an output-only window via an Electron shell later). The live-output affordance lives in the **global control area** (`#outputToolbar`), NOT the right sidebar. Clean-as-you-go IxD: while attention is on that toolbar, we relocate adjacent backlog cruft too. Web-first; each increment independently shippable; **the build that validates Syphon output in Arena bumps to v0.10.0** (milestone).

**Increment sequence:** 1 ✅ global control-area IxD cleanup → 2 ✅ stage spine + record-to-disk + live-output button → 3 ✅ output settings + dormant/active Syphon state + `?mocksyphon` mock host → **3.5 ✅ the docked output ROW** (replaces the popover; traffic-light LED; one-click record take; broadcast arm; gated Syphon name) → **3.6 ✅ app-bar restructure** (opaque in-flow bar scoped to the output column; output/canvas expand-bands; canvas controls relocated) → **4 ✅ Electron shell stands up** (new deps in `electron/` only) → **5 ✅✅ Syphon first light — VALIDATED IN ARENA (v0.10.0 milestone, Build 182)** → **6 ✅ multi-destination output + external window (Build 186, web-universal — superseded the Electron-only "second window" plan).** PHASE 0 DONE. **Next:** Phase 1+ (perform mode), or deferred infra: DMG packaging + Apple signing (no dev account yet), the global IxD/icon pass.

**Increment 3.5 — DONE (Build 177, no new deps; needs Daniel's verify).** The output controls moved from the anchored popover into a **solid docked row** ([#outputRow](../index.html)) above the work row, revealed by `#outputBtn` (the motion-footer pattern, top-docked): it takes layout space and pushes the preview down (re-fit in a rAF like the motion footer), not an overlay. **Traffic-light LED** on the output button (`.ot-led`, green broadcast over red record) is the at-a-glance state; the old top-right `#broadcastStatus` caption is gone, folded into the row. Row holds single-click: record take toggle (red), broadcast arm toggle (green; hidden on web, shown with `?mocksyphon`), resolution tier picker (follows `session.frameAspect`), Syphon name field **gated to Syphon hosts + labeled "syphon server name"** (fixes the inert-on-web control), live status folded in. `body.output-open` drops the floating toolbar's safe-area top so it sits under the band; coarse-landscape clears Safari's tab bar. [output-panel.js](../src/shell/output-panel.js) rewritten; `.output-popover`/`.output-broadcast-area` removed. Build green, `node --check` clean, no dup ids. **Verify (Daniel):** `output` toggles the docked band (preview re-fits, nothing overlays it); one-click record from the row lights the red dot + downloads on stop; plain web shows no broadcast/name controls; `?mocksyphon` shows broadcast + name, lights the green dot, fps + op records fill; resolution matches your comp aspect. **Locked decisions + the controls/IO inventory live in `docs/CONTROLS.md`.** Three scoped follow-ups remain in BACKLOG ("Global control-area UI follow-ups"), NOT part of 3.5: canvas-controls relocation (the global bar IS the home — a canvas button beside motion; mobile a settings button opposite flip-camera), source/output-swap relocation (near the divider), and a responsive/icon polish pass (icon+text → icon → "…" overflow).

**Increment 3.6 — DONE (Build 178, no new deps; needs Daniel's verify).** The app-bar restructure, rebuilt clean after last session's paused/reverted attempt. **`#mainSlot` is now a vertical stack** (the output column): an **opaque in-flow global bar** (`.output-toolbar`, dropped `position:absolute`/`pointer-events:none`) → contextual **expand-bands** `#outputRow` (live-output) + `#canvasRow` (canvas controls), one open at a time → the preview **stage** `#msStage` (flex:1, centers the canvas, holds `.output-status-area` + `#placeholder`). Bands push ONLY the preview down (re-fit in a rAF on toggle, like the motion footer); the right panel is never affected. **`wireBarBands` (chrome)** owns the accordion + re-fit + `.ot-btn.band-open`; `#outputBtn` (with its LED) toggles output, a new `#canvasBtn` (beside motion) toggles canvas. **Canvas controls relocated:** `wireBarBands` `appendChild`s the canvas `.group` (`#compZoom`/`#canvasRot`/`#oobModes`/`#frameAspect`) out of the right panel into `#canvasRow` at init — ids preserved so existing wiring (run earlier) stays intact; the empty `.controls` collapses (`.controls:empty`). **Measure fixes:** `resizePreviewCanvas` + `arrangeSlots` (both swap branches) measure/append `#msStage`. **Safe-area:** `.main-slot { padding-top: env(safe-area-inset-top) }`, coarse-landscape `34px`; bar `padding-top:16px` aligns buttons with the form row. `output-panel.js` dropped its band open/close (chrome-owned) and exposes `env.refreshOutputBand`. **Mobile is unaffected** (it replaces `document.body.innerHTML` and builds its own layout — confirmed independent of this index.html restructure). Build green, `node --check` clean, no dup ids. **Verify (Daniel):** opaque bar atop the output column; `output`/`canvas` expand subordinate bands that push ONLY the preview (right panel untouched); one band open at a time; preview re-fits on toggle; canvas controls (zoom/rotation/OOB/frame) work from the band; right panel ends at slice (no empty gap); iPad bar↔form-row alignment holds in both orientations; `?mocksyphon` broadcast still works.

**Follow-ups (BACKLOG "Global control-area UI follow-ups"), NOT in 3.6:** the **mobile** settings-button for canvas controls (mobile keeps canvas with slice until its own pass — its chrome rebuilds the body, so it needs its own change), source/output-swap relocation (near the divider), responsive/icon polish (icon+text → icon → "…" overflow as the bar grows).

**Increment 4 — DONE (Build 180, NEW dep `electron` confined to `electron/`; needs Daniel's on-device `npm start` verify).** The Electron shell stands up. New `electron/` subproject, fully isolated from the web app's `package.json` (which stays plain Vite + vanilla JS + GLSL). **[electron/main.js](../electron/main.js)** — a `BrowserWindow` (1400×900, `#0a0a0a`, `contextIsolation:true`, `nodeIntegration:false`) loading the built app from disk via `loadFile(../dist/index.html)`; vite's `base:'./'` (relative asset paths) makes `file://` resolve with no dev server. Detached devtools during bring-up; single-window lifecycle. **[electron/preload.js](../electron/preload.js)** — `contextBridge.exposeInMainWorld('foldHost', …)` of the SAME shape as `shell/host.js` `webHost` (`syphon`/`midi`/`nativeCamera`/`fileSystem`), `name:'electron'`, so `src/main.js`'s host seam (`window.foldHost` → `env.host`) sees a native host with zero app-side branching. **Syphon is a STUB** (`available:false`) — honest until the native module lands in Inc 5; it reports dormant, same as web (that's what `?mocksyphon` is for). A bring-up `console.log` confirms the preload ran. **[electron/package.json](../electron/package.json)** — `electron` only (resolved **42.4.1**). **`electron-builder` deferred** (only needed to package a distributable; nothing in Inc 4–5 needs it — filed in BACKLOG). `.gitignore` excludes `node_modules/` + `dist/`. Wrapper files `node --check` clean; Electron binary verified v42.4.1; web build unchanged + green. **Web functionally unchanged** (this is additive files under `electron/`; `dist/` is the same build). **Verify (Daniel, on device):** `npm run build` in repo root, then `cd electron && npm install && npm start` → the built Fold app loads in an Electron window, renders identically; devtools console shows `[fold-host] electron shell ready`; `window.foldHost.name === 'electron'`; the output surface shows Syphon dormant (no native module yet — correct).

**Increment 5 — VALIDATED IN ARENA (Build 182 = v0.10.0 milestone; NEW native dep `node-syphon@1.5.0` confined to `electron/`).** Daniel confirmed "Fold" live in Resolume Arena with live frames + live wedge manipulation. **OOM fix folded in (Build 182):** the first sustained run ran out of application memory after ~2 min — `ipcRenderer.send` queued ~15MB frame payloads unboundedly (renderer outruns main's Syphon upload). Fixed with backpressure: frame IPC is now `ipcRenderer.invoke` + `ipcMain.handle` + an in-flight flag (≤1 frame on the wire; excess frames dropped, which is correct for live output). **Re-test pending** — if memory still climbs, next suspect is node-syphon's per-frame Metal-texture handling (native module, not our IPC). **Crop issue (open):** Arena shows a proportionally-zoomed sub-region of Fold; our pixels are confirmed full-frame + correctly sized (`new Uint8Array(w*h*4)` from `readPixels`, [gl.js](../src/engine/gl.js)), so the crop is downstream — likely the Arena clip's transform/size mapping or an aspect mismatch between Fold's output resolution and the Arena comp. Diagnose by comparing a record-to-disk capture (identical frames): full + correct → Arena-side; same crop → our output aspect. The planned output test-pattern (BACKLOG) settles orientation + scale + aspect at once. The Electron shell publishes Fold's program frames as a Syphon source. Replicates the spike's validated combo (electron 42.4.1 + node-syphon 1.5.0, pinned exact). **[electron/syphon-bridge.js](../electron/syphon-bridge.js)** (new, from the spike's `syphon-server.js`) — a `SyphonMetalServer` created when broadcast is ARMED (carrying the editable name, so "Fold" only shows in Arena while live), `publishImageData` per frame, disposed on disarm/quit. **[electron/preload.js](../electron/preload.js)** — `syphon.available` → **true**; `start(name)`/`publish(pixels,w,h)`/`stop` → IPC to main (a `syphonStarted` gate as backup). **[electron/main.js](../electron/main.js)** — `ipcMain` handlers (`syphon:start`/`:frame`/`:stop`) drive the bridge; `will-quit` releases it. **[src/stage/syphon-sink.js](../src/stage/syphon-sink.js)** (new) `createSyphonSink(host)` — engine-agnostic bus sink (beside `recorder.js`, Fold-free); while ARMED forwards each frame to `host.syphon.publish`, `start`/`stop` arm/disarm + bring the native server up/down. **Gating is the perf-critical bit:** forwards only while armed, so the hot path (a full RGBA frame crossing the renderer→main boundary) runs only when live; a record-only session fans frames to the sink but they stop at a boolean (no copy/IPC). **[src/main.js](../src/main.js)** registers the sink only when `host.syphon.available`; **[src/shell/output-panel.js](../src/shell/output-panel.js)** routes the existing broadcast toggle through the sink's `start(name)`/`stop` (so the editable name reaches the actual Syphon server name — the spike hardcoded it). **No new UI** (output row / LED / broadcast toggle / name field all exist from Inc 3). `node --check` clean; node-syphon native binary loads + exposes `SyphonMetalServer`; web build green. **Web unchanged** (sink never registered without a native host). **Two things to verify on device (Daniel):** (1) **Y-flip** — our frames are raw bottom-up FBO pixels so the bridge publishes `flipped:false`; if "Fold" is upside-down in Arena it's a one-arg flip in `syphon-bridge.js`. (2) **fps** — two ~8MB copies per frame at FHD (renderer→preload→main); may land below the spike's ~95fps; zero-copy optimization is a BACKLOG follow-up, not first-light scope. **Verify steps:** `npm run build` in repo root, then `cd electron && npm install && npm start`; open Arena → arm **broadcast** in the output row → "Fold" appears with live frames; manipulate the wedge → Arena updates live. **On your confirmation → v0.10.0** (I stamp it on the confirmation/flip-fix build).

**Increment 6 — DONE (Build 186), web-universal instead of Electron-only.** The "output-only second window" became a universal external-window sink ([stage/window-sink.js](../src/stage/window-sink.js), `window.open` + canvas) that works on web AND Electron — no native second window needed. Shipped with the multi-destination output model (picker + start/stop). See the Build-186 note up top. **Next options:** Phase 1+ (perform mode), or deferred infra (DMG packaging + Apple signing — no dev account yet; the global IxD/icon pass).

**Increment 1 — DONE (Builds 171–174, four iterations, behavior-preserving, Daniel verified the UI; orientation tweaks need an on-device confirm).** Build 174 finished the toolbar polish: the status caption shows the current source as `filename · dims`; the form picker matches `.ot-btn` size (40/44px), drops the "form" label, and aligns with the toolbar (landscape right-panel `padding-top` 16→34px fixes the form-icon drop); `.ot-btn` got `white-space: nowrap` and portrait-coarse falls back to flex `space-between` to stop the "save ▸" wrap + undo/redo cramp. The right sidebar is now **segment/slice/canvas only**; the global control area (`#outputToolbar` + modals) owns source input + app actions. The output toolbar is a **3-column grid** (`1fr auto 1fr`): **left = source input** — upload/camera inline (one tap to the OS picker; no modal — the Build-172 `source ▸` modal was reverted after Daniel's iPad test found the extra tap), swapped for the inline **capture/flip/stop** live group while the camera runs (`updateCameraUI` toggles the shared left slot); **center = undo/redo**, pixel-centered (stays put as side widths change, e.g. camera live); **right = swap / motion / `save ▸` / version chip**. `save ▸` → `#exportSheet` (export controls verbatim + Firefox-cap notice; feedback in-modal via `#exportStatus`, like the render sheet). The compact **version chip** (`.ot-version`, merges the old version badge — always-visible discrete-diagnostics affordance) → `#diagSheet` (`#diag` readout + run-diagnostics + a new `profile: engine · capture · maxFBO` capability line; full report/copy-JSON via `showDiagnosticPanel`). The source/status caption (`.output-status-area`) sits under the left source controls (contextualized): `#status` (current source + live state; engine-init error target) + `#uploadError` (codec/secure-context errors), each empty-collapsing. Sheets reuse `.vid-sheet` (open=`hidden=false`, ✕/backdrop closes). All relocations keep original ids → wiring untouched. Builds green, `node --check` clean, no duplicate ids. **Verify:** upload/camera one tap (no modal); start camera → capture/flip/stop in the left slot + "live camera" caption below, capture a still; undo/redo centered and don't jump on camera toggle; `save ▸` → export telemetry in the modal; version chip → diagnostics; right sidebar = form/thumbnail/slice/canvas only. **Open IxD follow-ups (minor, deferred):** the initial "load an image to start" caption is slightly redundant with the centered placeholder; the iPad orientation tweaks (Build 174) want an on-device confirm.

**Increment 2 — DONE + VERIFIED (Build 175, no new deps; Daniel confirmed files save to disk on iPad Safari + Brave desktop).** The stage spine is in. **Perf finding (expected, not a blocker):** the web record path runs ~11-12 fps and lags motion playback, independent of source resolution (confirms the bottleneck is output-side, not source). This is NOT representative of Syphon: the record path pays three costs Syphon won't — real-time MediaRecorder encode, a second render pass competing with the motion preview, and the slow `readPixels` + CPU Y-flip (vs the engine's fast `drawImage` capture). The spike's ~95fps@1920² remains the Syphon predictor. The web recorder is a UX stand-in; its fast path (render-to-canvas + drawImage instead of readPixels+flip) is filed in BACKLOG, not done now. **Engine:** `exportFrameRaw(state, w, h)` in [src/engine/index.js](../src/engine/index.js) returns raw bottom-up RGBA straight from `renderToFBO` (no 2D copy / Y-flip / encode), timings passed through; sinks flip per-sink. **New `src/stage/` layer (engine-agnostic, zero kaleidoscope assumptions):** [engine-adapter.js](../src/stage/engine-adapter.js) is THE CONTRACT (the whole coupling surface) — universal `renderFrameAt(w,h) → Frame`; perform-tier `getState/applyState/tween` (Phase 2); `hasPerformTier()` gates perform features. [output-bus.js](../src/stage/output-bus.js) `createOutputBus({ engineAdapter, host, diag })` — paced rAF loop renders one frame at the output resolution (default **1920²**, the spike's proven-viable size) and fans it to registered sinks (`{ id, publish(frame) }`); owns `{width,height,aspect}`/fps/serverName; `registerSink`/`start`/`stop`/`setResolution`/`setServerName`/`getStatus`; idle = stopped; pushes one `op:'live-output'` record per ~1s window to `diag.ops`. [recorder.js](../src/stage/recorder.js) `createRecorderSink()` — the first sink: hidden 2D canvas + `MediaRecorder` over `captureStream()`; `publish` Y-flips into the canvas, `start(w,h)`/`stop` → downloadable file (MP4 where supported, else WebM). **Diagnostics substrate:** [kit/op-ring.js](../src/kit/op-ring.js) `createOpRing(120)` → `env.diag.ops`; the diagnostics sheet's `#diagOps` shows recent records (refreshed on open). **Fold wiring:** [shell/fold-adapter.js](../src/shell/fold-adapter.js) `createFoldAdapter(env)` (in shell/ so stage/ stays Fold-free) — `renderFrameAt` reads `env.state` LIVE via `exportFrameRaw`; perform tier over `env.state` + `kit/tween.js`. **Host seam:** [src/main.js](../src/main.js) now `createApp(env, { capabilities, host: window.foldHost })`, then builds the adapter + bus + registers the disk sink + mounts the output panel; `env.outputBus` + `env.diag` set. (`?mocksyphon` branch is Increment 3.) **UI:** a persistent **output** button in `#outputToolbar` (gated on a loaded source via `env.updateOutputUI`, called from `arrangeSlots`; `.active` while recording) opens `#outputSheet` ([shell/output-panel.js](../src/shell/output-panel.js)) with record start/stop + a live `W×H · fps` readout (polled 500ms). Web functionally unchanged (bus idle until you record). `ARCHITECTURE.md` updated (new **Stage** layer in the vocabulary + directory map + `env.diag`/`outputBus`). **Verify (Daniel):** load a source → **output** → **record to disk** (readout shows live fps + dims) → **stop recording** downloads a playable file; open diagnostics → recent live-output op records appear. **Design choices made (flag if you'd change them):** output default 1920² square; op records are 1s aggregates (not per-frame, so the 120-slot ring spans ~2 min); record stops the bus on stop (the disk sink is the only consumer in Inc 2 — Syphon will keep the bus running independently in Inc 5).

**Increment 3 — DONE (Build 176, no new deps; needs Daniel's verify).** Output settings + dormant/active Syphon state + the surface relocated out of the modal, folding in Daniel's Build-175 feedback. **Surface relocated (his #4):** the `.vid-sheet` modal is gone — live status is a **persistent top-right readout** ([broadcastStatus](../index.html), `.output-broadcast-area`, mirroring the source/input caption top-left), and controls are an **anchored dropdown** ([#outputPopover](../index.html), positioned off the `#outputBtn` rect by the panel, no backdrop) so status stays visible while editing. **Output honors frame aspect (his #3):** [output-panel.js](../src/shell/output-panel.js) long-side tier picker (HD/FHD/QHD/4K, default FHD, 4K = "clean hardware only") with aspect FOLLOWING `session.frameAspect` (same math as `engine.exportAt`) → 16:9 comp gives 1920×1080; never square 4K. **Dormant/active Syphon:** [src/stage/mock-host.js](../src/stage/mock-host.js) `mockSyphonHost` (webHost shape, `syphon.available:true`, counting no-op publish) wired via `?mocksyphon` in [main.js](../src/main.js); the broadcast indicator + toggle reflect `env.host.syphon.available` (dormant on plain web → toggle hidden). **Editable server name** → `outputBus.setServerName`. **Bus lifecycle:** runs while recording OR broadcasting (stopping one doesn't tear down the other); resolution locked while running. The output panel owns the popover open/close (not `wireGlobalSheets`). Build green, `node --check` clean, no dup ids. **Verify (Daniel):** plain web → `output` opens the dropdown (no blurred modal), top-right shows `output W×H` matching the comp aspect, record-to-disk works, broadcast dormant; `?mocksyphon` → broadcast toggle appears, "start broadcasting" lights the indicator + fps + fills the diagnostics op records, picker + name editable.


## SESSION WRAP 2026-06-17 — Fold Live program spec approved (planning session, no code)

**This was a brainstorm-to-spec session. No code shipped. The next session starts Phase 0.**

We turned the green-lit Electron+Syphon spike into an approved, phased program for the **live performance shell** ("Fold Live"). The full spec is the durable artifact: `~/.claude/plans/in-our-last-thread-splendid-sparkle.md`. Read it first next session, then this HANDOFF, then ARCHITECTURE/BACKLOG as needed.

**The shape (decided with Daniel):**
- Fold becomes an instrument you play, via a **distinct perform mode** (program/preview, big output, peripheral controls) that reuses the engine + tween/keyframe kit + components. Two sibling live products share it: the Mac/Electron-Syphon rig (primary) and a later mobile/Capacitor HDMI surface.
- **One program frame, many sinks**: render once to FBO at output resolution (`renderToFBO`, NOT the display canvas), fan out to Syphon / an output-only second window / record-to-disk.
- Built as an **engine-agnostic `src/stage/` layer** (first tenant Fold) with a small engine-adapter contract (`renderFrameAt` universal tier; `getState/applyState/tween` perform tier). Daniel has N adjacent Electron-to-Arena app ideas (gesture light, zoetrope, audio-viz); the call is decoupled apps sharing a library, NOT a monolith and NOT N reinvented Syphons. Do not extract a package until a second tenant exists.
- Native host injects via the existing one-line `createApp(env, { host })` seam (`window.foldHost` set by the Electron preload; web build byte-identical).
- **Diagnostics is promoted into Phase 0** (capability-line readout + unified `env.diag.ops` record + a `live-output` op shape + copy-diagnostics report). The output-status chrome is built ON it. This is how the Chromium-perf question gets settled with data, not vibes.

**Roadmap (each phase is roughly one arc):** Phase 0 Electron+Syphon foundation + stage layer + diagnostics, then 1 bounded motion-IxD cleanup (clean-as-you-go, non-blocking), 2 perform-mode core (program/preview + take/transition), 3 live gesture + smart-tween, 4 single source as a ready asset, 5 A/B crossfade (the deck's engine), 6 multi-slot deck (library UI over A/B), later Capacitor/iOS HDMI.

**Next session = Phase 0** (detailed in the plan, including critical files + end-to-end verification against Arena). New `src/stage/` + `electron/` subproject (own deps: electron, electron-builder, node-syphon; web app stays plain Vite + vanilla). A Movink touch display plus a second monitor is Daniel's desktop rehearsal rig for the eventual iPad/iPhone touch UX (validates ergonomics, not the iOS HDMI signal path).

## SESSION WRAP 2026-06-17 — Electron+Syphon spike complete

**Spike verdict: green light.** The CPU readback path (drawImage + getImageData in a preload RAF loop → IPC → SyphonMetalServer) is viable for live VJ output on Apple Silicon. End-to-end confirmed: Electron → SyphonMetalServer → Resolume Arena showed live kaleidoscope frames as "Electron - Fold."

**Benchmark results (M5 Max, Apple Silicon unified memory):**

| Resolution | fps | Capture time | Viable? |
|---|---|---|---|
| 960×960 | ~120 fps | ~2.6ms | Yes |
| 1920×1920 | ~95 fps | ~8.5ms | Yes |
| 3840×2160 (16:9 4K) | ~41 fps | ~18.9ms | Yes |
| 3840×3840 (square 4K) | ~23 fps | ~24ms | No (below 30fps) |

Bottleneck is GPU→CPU pixel readback (getImageData), not IPC. Cost scales linearly with pixel count. Apple Silicon unified memory is the key enabler — no PCIe bus crossing. M1+ should handle 1920×1920 comfortably; 4K 16:9 suits M1 Pro/Max and up. Intel Mac likely not viable.

**Important caveat:** the spike captured from the display canvas, which stayed at 300×150 in Electron (the app's resize logic doesn't fire the same way there). The real integration must use `renderToFBO()` in `engine/gl.js` — that path renders at arbitrary resolution independent of the display canvas. Syphon throughput numbers are accurate regardless (getImageData cost is set by the capture target size, not the source canvas size).

**Integration path for the real Electron build:**
- Implement `nativeHost.syphon` against the `shell/host.js` seam (`available`, `start`, `publish`, `stop`)
- Use `engine.renderToFBO(w, h)` for frame capture at the target Syphon resolution
- Use `SyphonMetalServer` (not `SyphonOpenGLServer`) — Metal works in Electron's main process; the OpenGL server fails due to GPU process isolation
- Serve built `dist/` instead of the Vite dev server; package with electron-builder
- Call `createApp(env, { host: nativeHost })` — the seam is already defined and waiting

**Spike code:** `spike/electron-syphon/` — keep as reference for the native addon wiring. The `syphon-server.js` file in particular seeds the real integration (SyphonMetalServer init/publish/destroy pattern is proven). `node-syphon@1.5.0` is the npm package (GPL-3.0+, ships pre-built arm64 binary + Syphon.framework).

**Next for the native Electron build:** separate project, not a continuation of the spike. The spike is throwaway except for the Syphon wiring pattern. The architecture is ready (shell/host.js seam, createApp mount point, engine FBO path). When Daniel is ready to build it for real, the plan is in BACKLOG Phase 4.

## SESSION WRAP 2026-06-10 — state + prioritized next (read this first)

**Where we are.** The source-video animation track (the big arc of this session, plan `~/.claude/plans/we-just-finished-a-sorted-mist.md`) is feature-complete: load a video → scrub/keyframe/play over moving footage → frame-accurate export; a navigable timeline (zoom/pan/fit, ticks/timestamps, total duration, smooth playhead-follow); video retime (25/50/100/200%); and a **pre-animation clip editor** (trim + bounce + slice/crossfade bake, all behind a destructive warning) with smooth in-editor previews (scrubber, bounce, two-video crossfade dissolve). Bake is seek-based (fine for short loops; fast-bake via WebCodecs decoder is deferred). Everything below is the queue Daniel + I aligned on at the end of this session — roughly in priority order, but Daniel chooses.

**Chosen direction (Daniel, 2026-06-10).** Primary next track = **engine/perf hardening** (item 1 below is least-ambiguous: perf + stability + compatibility wins + new capabilities like frame interpolation / slower speeds; collaborative IxD + Claude-driven technical). Running in PARALLEL (Daniel's lanes, not blockers): the **motion-controls IxD pass** + the **bug/polish/wishlist cluster** (sub-group it — quick wins like the still-package SVG overlay + icon cleanup vs the expensive cross-cutting Droste controls), the **UI/brand pass** (any time, until we integrate Daniel's detailed notes / a Plasmic workflow), and Daniel's own **alpha-test + market/UX research** (informs positioning D1 / distribution D3). Then **+gesture record + live-tween** builds on the hardening (after motion IxD cleanup). Daniel's personal milestone = **Syphon out** → see the new BACKLOG analysis "Native wrapper / Syphon — does going native FORK the code?" — short answer: NO, a wrapper (Electron/Capacitor) reuses this whole web codebase; an **Electron+Syphon spike** (no Apple license) is the de-risking experiment, sequenced engine-hardening → realtime-to-disk → spike → native features. ARCHITECTURE.md updated this session (layering, motion/timeline, video seam, clip editor, native-reuse story).

1. **Motion-controls UX / ergonomics / discoverability pass (Daniel's top-of-mind; IxD-driven — Daniel drives).** The motion footer has grown organically and is crowded; time to tidy + dial in. Concrete starting points already noted: switch the retime **speed presets to a single-select menu/dropdown** (frees horizontal space, more intuitive) + **add 150%**; group controls by role (transport / keyframe-edit / view-zoom / output / source-prep); an unambiguous **loop on/off** affordance; better **prev/next stepper** ergonomics; pixel-precise marker/scrubber polish; discoverability of the **clip ▸** and retime controls; the ⋯ menu. North star (from BACKLOG): Procreate Dreams / iMovie — uncluttered, precise, powerful, not cartoonish. Natural consolidation point after all the feature work.

2. **Long render/bake → stream to disk (OPFS) + size warning — REQUIRED for the 10-min/4K target.** Still necessary (Daniel's question: yes). The muxer uses `fastStart:'in-memory'`, accumulating ALL encoded chunks until `finalize()` — a 10-min/4K render (or a long bounce/slice bake) will OOM/hang. Fix: stream encoded output to **OPFS** (`createSyncAccessHandle` in a worker, cross-browser) then download the disk-backed file; add a pre-render byte estimate (bitrate×duration/8) that warns on extreme jobs. NOTE (established this session): this buys MEMORY + responsiveness + abort-ability, NOT throughput (throughput is browser/CPU-bound; the native wrapper is the only real multi-core/HW-encode path). Benefits BOTH the main render and the clip bake (both use the in-memory muxer today).

3. **Firefox video color + orientation pass (real correctness bug).** iPhone `.mov` shows washed-out OUTPUT color on Safari + Firefox (WebGL texture colorspace/range — the 2D source preview shows natural color, so it's the texture path; investigate `UNPACK_COLORSPACE_CONVERSION_WEBGL` / limited-vs-full-range YUV→RGB / HDR). Firefox additionally applies a 90° CCW rotation to ALL videos + an iPhone aspect squish (Gecko rotation/pixel-aspect metadata). Brave/Safari are reference-correct. Needs reading rotation metadata + normalizing in the upload/draw.

4. **Cross-format / frame-rate / resolution robustness + a test story.** The 60fps finding (slower seeks; a once-seen out-of-sync start/end keyframe) showed non-30fps is under-tested. Want: estimate source fps (rVFC frame-delta; shared with the retime hint), robust exact-frame seeking at higher rates, and a deliberate **test harness/checklist** across codecs (H.264/HEVC/ProRes-Safari-only), frame rates (24/30/60/VFR), resolutions, and engines (Blink still largely untested for the video path). Pairs with the developer-tooling backlog (visual-regression harness, `npm run check`).

5. **Frame interpolation + slower-than-25% playback.** Retime confirmed: 25% is unusable at 30fps source, 50% choppy. Do the cheap **Tier-1 source-fps hint** first (warn when a preset drops effective fps below ~15). Then blend interpolation (medium, fits a baked pipeline) unblocks sub-25% speeds; optical-flow is the someday/native-wrapper version.

6. **Real-time animation from the live camera (iPad/mobile) + gesture recording — revisit with fresh planning (Daniel wants this back on the radar).** Live-transition tweening (smart-ease between current and new settings during direct manipulation, reusing the Phase-3 tween kit) and a **gesture-record mode**: record the continuous parameter stream while manipulating, detect the return-to-start loop point, smooth/simplify to sparse keyframes. This is ALSO the home for the **"follow my movement, not the nearest path"** need — e.g. rotating ~360° between keyframes should capture the *intended winding* (the per-segment rotation-winding item: default shortest-path, opt-in +N turns, plus captured winding from record mode). Especially valuable once Fold's output Syphons into Arena. Larger, shared across the three planned apps — worth a planning session of its own.

**Tech-debt / perf notes (my context, for the next session):** export/bake throughput is browser/CPU-bound (per-frame `VideoFrame` color conversion on Safari ~177ms; seek cost on Firefox) — the muxer-to-disk worker fixes memory/responsiveness but not speed; real speed is the native wrapper (FOLD.md Phase 4) or, for the bake specifically, a WebCodecs `VideoDecoder` + `mp4box.js` demuxer (NEW dependency, needs approval). The clip bake's per-frame seeks + the in-memory muxer are the two bake-perf levers. Blink/Chromium remains the least-tested engine for the whole video path.


**Architectural prep Phase 4 — host-services seam; ARC COMPLETE (Build 170, no verification needed — pure scaffolding).** `src/shell/host.js` defines the host-services interface (`syphon`/`midi`/`nativeCamera`/`fileSystem`) + ships `webHost`, the web no-op (every service `available:false`, safe stub methods). Threaded onto `env.host` via `createApp` (defaults to webHost; native shells inject their own). Nothing reads `env.host` yet — the seam is defined for future Syphon/MIDI/native-save features + the native shells. Build green, node --check clean. **The architectural-prep arc (plan `~/.claude/plans/we-re-doing-architectural-preparation-indexed-peach.md`) is DONE — Builds 164–170:** P1 closure→env; P2 main.js split → desktop chrome + clip/source/motion modules behind `env` handles + `createApp`; P3 `kit/capabilities.js`; P4 `shell/host.js`. **What a native wrapper does now:** Electron/Capacitor reuse the whole web app and call `createApp({ host: nativeHost, capabilities: nativeCaps })` — no fork. Next native step per BACKLOG is the **Electron+Syphon spike** (de-risk the WebGL-output→Syphon path); the `host.syphon` interface is the seam it implements against. **Open (filed, NOT refactor-related):** the Build-167 PWA findings (safe-area double, lose-preview-after-save) + the Build-164 cross-engine findings (Firefox export stutter, iPad timeline-shrink) — all in BACKLOG.

**Architectural prep Phase 3 — capability profile (Build 169).** `src/kit/capabilities.js` `createCapabilities(engine)` → one probe-once table (`engineId`, `capturePath`, `maxFBOSize`/`maxTextureSize`, `firefoxTextureCapped`). Built in main.js, threaded onto `env.capabilities` via `createApp`. Replaced the scattered UA sniffs with profile reads: capture path (was `defaultCaptureMode`) → `env.capabilities.capturePath`; Firefox 8K cap (was `isFirefox`/`isFirefoxCappedAt8K`) → `env.capabilities.firefoxTextureCapped`. Byte-identical decisions (same regexes/override/test). Already-single-sourced probes (videoExportSupported/pickVideoCodec, seekVideoTo, gl.js readback) stay put — capabilities.js indexes them. Build green, node --check clean. **Verify:** Safari = `gl` path + no FF notice; Firefox = 8K notice + upload hint + `2d`; `?capture=` override works. **Next: Phase 4** — `shell/host.js` interface + `webHost` no-op (Syphon/MIDI/native-camera/file-system; everything reports unavailable on web). It threads onto the `host` param of `createApp` (currently null). Then the architectural-prep arc (plan `~/.claude/plans/we-re-doing-architectural-preparation-indexed-peach.md`) is done.

**Architectural prep Phase 2d — `createApp` mount seam; PHASE 2 COMPLETE (Build 168).** Added `src/shell/app.js` `createApp(env, { host, capabilities })` — chrome builds env, then `createApp` threads the injectable host/capability seams onto env and mounts the wiring (clip → source → motion) in one call. main.js init is now chrome-handle assignments + one `createApp(env, …)` (was three inline createX calls); imports `createApp` not the three modules. host/capabilities default null (Phase 3/4 fill them); the seam exists so Electron/Capacitor inject their own without editing the app. Lean orchestrator by design — wrappers reuse main.js's chrome as-is, so they need the injection seam, not duplicated engine/env construction. Build green, node --check clean. **Phase 2 done: app wiring fully separated from desktop chrome (main.js 2636→~865 lines + shell/app.js + clip-editor.js + source-host.js + motion-runtime.js, all behind env handles + createApp).** ARCHITECTURE.md updated. **Next: Phase 3** — `kit/capabilities.js` (centralize the per-engine matrix: capture path / codec ceiling / FBO caps / readback / seek / orientation / ProRes / WebCodecs across the 5 runtimes; the matrix is in the plan + BACKLOG). **Then Phase 4** — `shell/host.js` + `webHost` no-op (Syphon/MIDI/native-camera/file-system seam). Plan: `~/.claude/plans/we-re-doing-architectural-preparation-indexed-peach.md`.

**Architectural prep Phase 2c — motion runtime extracted; main.js is now the desktop chrome (Build 167).** The whole motion subsystem (~1,100 lines) moved to `src/shell/motion-runtime.js` (`createMotionRuntime(env)`) — sampling/playback, keyframes, timeline (ruler/markers/view-transform/filmstrip), scrub/retime, motion-JSON, and the video-export sheet. Done via **byte-exact extraction** (line-range copy, not retype) + mechanical `env.`-prefix conversions, to protect the byte-for-byte invariant. **main.js: 2203 → 865 lines** — init is now three `createX(env)` calls (clip → source → motion) + chrome wiring. Cross-module audit confirmed every `env` handle motion consumes is set (chrome top-level assignments run before init; module handles set during init, consumed at event time). `node --check` + build green; dropped 10 unused imports. Build 167 includes 2a+2b. **Verify (the big one):** full motion/keyframe/timeline/playback pass, filmstrip, pinch-zoom-pan-fit on iPad, motion-JSON save/load, **video export** (sheet + gating + render + .zip package), source-video scrub/play/retime — plus clip editor + camera/still-export still working (they call motion via `env`). **Remaining Phase 2:** 2d `shell/app.js` `createApp({host,capabilities,dom})` orchestrator (wrap the engine+env construction + the createX calls) + final main.js chrome slim-down. **Then** the `docs/ARCHITECTURE.md` update (module structure + createApp/env-handle seam — Daniel's request).

**Architectural prep Phase 2b — source host extracted (Build 166).** `src/shell/source-host.js` `createSourceHost(env)` now owns media loading (loadImage/loadVideo/stopSourceVideoPlayback), the live-camera host (getUserMedia + render loop + flip + capture — creates its own `camera`, wires its own buttons) and still export (doExport/exportPackage). Same convention as 2a (late-bound `env.X()` to collaborators; public surface on `env`). main.js exposes the motion/chrome collaborators it still owns on `env` in the init block (`haltPlayback`/`rebindMotionToSource`/`updateMotionUI`/`scrubVideo`/`renderTimeline`/`ensureSeededSelection`/`lockVideoDuration`/`stopPlayback`/`fmtClock`/`drawMiniKaleidoscope`/`sourceOverlay`/`isFirefoxCappedAt8K`); dropped unused `createCamera`/`getActiveForm` imports. main.js 2636 → 2203 lines. Build green, static-checked. **166 includes 2a** (clip editor), so testing 166 covers both extractions. **Verify:** image+video upload (drag-drop + picker), still export (PNG/JPG + .zip package), live camera start/flip/capture/package (iPad + phone PWA), source-switch-mid-animation rebind, AND the clip-editor pass from 2a. **Remaining Phase 2:** 2c `shell/motion-runtime.js` (the largest — motion core + timeline + filmstrip + video-export sheet; ~900 lines, most interconnected) → 2d `shell/app.js` `createApp({host,capabilities,dom})` orchestrator + slim main.js to desktop chrome. **Then** update `docs/ARCHITECTURE.md` (Daniel's request — once the structure settles).

**Architectural prep Phase 2a — clip editor extracted (Build 165).** First physical module extraction. The clip editor (`#clipSheet` trim/bounce/slice + bake) moved from `main.js` to `src/shell/clip-editor.js` as `createClipEditor(env)`. **Convention established for the rest of Phase 2:** extracted modules call collaborators via late-bound `env.X()` handles and expose their own public functions on `env`; stable refs (`engine`/`motion`/`session`) are destructured. main.js now exposes the clip editor's collaborators on `env` in the init block (`env.stopPlayback`/`ensureSeededSelection`/`renderTimeline`/`updateMotionUI`/`scrubVideo`/`lockVideoDuration`/`stopSourceVideoPlayback`/`fmtClock`) — these still live in main.js for now and migrate to their own modules in 2b/2c, but the `env` seam stays stable across those moves. Pure move, build green, static-checked (no bare cross-module refs). **Verify:** clip editor trim/bounce/slice/scrubber/cancel on a video, identical to Build 164. **Remaining Phase 2:** 2b `shell/source-host.js` (media load + camera + still export) → 2c `shell/motion-runtime.js` (motion core + timeline + filmstrip + video export) → 2d `shell/app.js` `createApp({host,capabilities,dom})` orchestrator + slim main.js to desktop chrome. **Then update `docs/ARCHITECTURE.md`** to reflect the final module structure + the createApp/env-handle seam (Daniel's request — do it once the structure settles, end of Phase 2).

**Architectural prep Phase 1 — closure→env migration (Build 164, verified by Daniel on the deployed build — no regressions; the cross-engine observations he found were all pre-existing, filed to BACKLOG 2026-06-16).** First refactor toward native wrappers (plan: `~/.claude/plans/we-re-doing-architectural-preparation-indexed-peach.md`). Pure behavior-preserving MOVE: every module-level mutable `let` in `main.js` that wiring functions closed over now lives on cohesive `env` runtime sub-objects, so no wiring function touches module scope (prerequisite for relocating it without forking). New `env` groupings: `media` (sourceFilename/sourceVideoUrl/originalSource/captureObjectURL), `live` (isLive/active/raf), `motionRT` (active/raf/start/scrubbing/pointers/gesture/relayoutPending), `clip` (`trim` object + preview/bake machine: prevVideo/prevVideoB/backup/drag/raf/seg/bounceStart/phase/seekT/seeking/baking), `filmstrip` (timer/lastSig/gen/busy/freezeEl), `scrub` (seekP/seeking/assign), `sched` (renderScheduled/syncCtrlScheduled), `sourcePreview` (frame/overlay/parent). `state`/`session`/`motion` + the `env.sourceVideo`/`env.liveVideo` handles unchanged. **Identifier note for future sessions:** prior HANDOFF/CHANGELOG entries name old locals (`_clipPrevVideoB`, `_filmstripGen`, `motionActive`, `clip.inT`, …) — these are now `env.clip.prevVideoB`, `env.filmstrip.gen`, `env.motionRT.active`, `env.clip.trim.inT`, etc. `npm run build` passes. **Verify:** the full gesture/flow matrix must be byte-for-byte identical to Build 163 (overlay gestures, output pinch/twist, form switch/swap, divider, undo/redo, still export, motion/timeline/keyframe, video load/scrub/play/export, clip editor trim/bounce/slice, iPad live-camera start/flip/capture/package). **Next (Phase 2, mechanical):** extract the wiring into `shell/motion-runtime.js` / `shell/clip-editor.js` / `shell/source-host.js` + a `shell/app.js` `createApp({host,capabilities,dom})` orchestrator that the desktop chrome and a future Electron/live shell both mount.

**Smooth crossfade preview — two-video live blend (Build 163, needs Daniel's verify).** Replaced the stuttery seam-frame capture with a second hidden preview `<video>` (`_clipPrevVideoB`) playing the A-head alongside the main B-tail, alpha-blended on `#clipBlend` (`drawTwoVideoBlend`/`startSlicePreview`). Both 1× → smooth at any crossfade length; no capture, no adjust-pause; the post-crossfade main seek is masked by the blend (no end flicker). Removed `captureSeamFrames`/seam buffers. **Bake-speed decision (Daniel):** seek-based bake kept (flicker covered, 162) — fine for short loops; WebCodecs `VideoDecoder` + `mp4box.js` (new dependency) is the future fast-bake if long-clip speed becomes a blocker; decode-by-playback shelved (narrow/memory-walled — see BACKLOG). **Clip editor previews fully done** (scrubber, bounce, smooth crossfade). **Next candidates:** loop-lock (last plan item), retime UX (single-select + 150% + fps hint), or whatever Daniel surfaces.

**Bake "baking…" cover (Build 162).** `#clipBaking` cover hides the clip stage during the bake so it doesn't flicker (the bake seeks the visible video). Fixes Daniel's issue 2 independent of decode method.

**BAKE-SPEED INVESTIGATION (2026-06-10) — decode-by-playback is narrower than the plan assumed; needs a decision.** Findings: (1) it's ≈3× ONLY on Firefox/Chromium (where `VideoFrame`-from-canvas is cheap); **Safari is encode-bound** (~177ms/frame `VideoFrame`), so faster decode doesn't help there and real-time capture would outpace encode → memory buildup. (2) **Bounce can't use it** — the reverse needs the whole forward pass buffered at source res (a 5s 1080p clip ≈ 1.2GB) → infeasible; bounce stays seek-based. (3) **Slice's crossfade seam buffer scales with crossfade DURATION × source res** — 0.5s ≈ 120MB (ok), 3s ≈ 720MB (risky). So decode-by-playback is a narrow, conditional, memory-caveated ≈3× win. **The real fast fix = WebCodecs `VideoDecoder` + mp4 demuxer (`mp4box.js`, NEW dependency — needs Daniel's approval).** **Reframe:** the smooth dial-in Daniel needs is best served by the **two-video crossfade PREVIEW blend** (play B-tail + A-head simultaneously, alpha-blend — smooth, no capture, fixes issue 1 stutter, independent of bake speed). The seek-based bake is acceptable for SHORT loops (the common case; the 1-min clip was an edge); pursue the WebCodecs decoder only if long-clip bake speed becomes a real blocker. **NEXT (pending Daniel):** two-video crossfade preview blend (recommended) and/or WebCodecs-decoder bake (dependency decision).

**Clip editor: live crossfade dissolve preview (Build 161, needs Daniel's verify — eyes on timing/look).** Slice preview is now a phase machine (`startSlicePreview`): native B → pause + cross-dissolve pre-captured seam frames on `#clipBlend` over the crossfade duration → seek to in+cfSec → A → loop. Small seam buffer (`captureSeamFrames`, ~8 frames/side preview-res, seek-based + debounced via `scheduleSeamCapture` on slice-enter / cut / crossfade / trim change); hard-cut fallback until captured. Faithful to the baked output. Known: brief preview pause (~1–2s) while re-capturing the seam on changes; live dissolve is preview-res. **All three previews now done** (scrubber 159, bounce 159, crossfade 161). **NEXT — the remaining half: bake speedup (decode-by-playback).** Replace `bakeAndApply`'s per-frame seeks with sequential play-through capture (rVFC / playback-timer; `captureRangeByPlayback` primitive); slice = two forward passes streaming + small crossfade ring buffer (low memory); bounce = forward + buffered reverse (source-res → size cap/warn). Plan: `~/.claude/plans/we-just-finished-a-sorted-mist.md`.

**Two fixes (Build 160, needs Daniel's verify).** (1) Clip scrubber resumes from the scrubbed position (`startClipPreview(reset=false)`), not the segment start. (2) Video plays/renders with ≥1 keyframe (`env.sourceVideo ? 1 : 2`) — the default seeded kf0 is now playable (footage moves under static params); stills still need ≥2. **NEXT (Daniel reframed):** crossfade preview is ESSENTIAL (the editor's whole point is dialing trim/crossfade BEFORE baking) — do BOTH the live previews AND the bake speedup, sequenced to minimize complexity. Recommended path = one shared **decode-by-playback** primitive (`captureRangeByPlayback`): capture the trimmed range once → a preview-res buffer drives SMOOTH bounce/slice/crossfade previews (blend buffered frames; no seeking), and the SAME capture at source res streams to the encoder for the fast bake. Plan: `~/.claude/plans/we-just-finished-a-sorted-mist.md`.

**Clip editor previews + scrubber (Phase 1a, Build 159, needs Daniel's verify).** Plan: `~/.claude/plans/we-just-finished-a-sorted-mist.md` (clip-editor previews + bake speedup). (1) **Scrubber** — drag `#clipBar` off the handles → coalesced `clipSeekTo` + pause/resume the preview. (2) **Bounce preview** now seek-driven (wall-clock triangle → `clipSeekTo`), so it bounces (reverse choppy on long clips; scrubber inspects the turnaround). **NEXT: Phase 1b** — real crossfade dissolve in the slice preview (pre-capture the short seam windows at preview res + `#clipBlend` overlay). **Then Phase 2** — bake decode-by-playback (root perf fix: replace per-frame seeks with sequential play-through capture; slice two-pass streaming, bounce buffered-reverse + size warn).

**Clip editor B2 — slice + crossfade bake (Build 158, needs Daniel's verify).** Slice mode live: cut-point handle (blue) + crossfade-duration field; bake rearranges B(=[cut,out])+A(=[in,cut]) and cross-dissolves the B→A seam (overlap, two seeks/crossfade-frame). Preview plays the rearrangement (hard seam live; blend on bake). **Clip editor is now feature-complete (trim / bounce / slice).** Remaining clip-editor polish (deferred): seek-based decode is slow on long clips (WebCodecs-decode speedup), 30fps bake (source-fps estimation), no mid-bake cancel, live preview shows hard seam + bounce preview is forward-only. **NEXT options:** loop-lock (last plan item), retime UX (single-select + 150%), frame-interpolation (priority raised), seek-reduction (footage cache).

**Fix: selected kf0 after clip editor / source switch (Build 157, needs Daniel's verify).** Daniel found that after trimming, the first `+keyframe` re-created a start keyframe instead of adding an in-between (the re-bind set `selected=-1` and didn't guarantee kf0). New `ensureSeededSelection` guarantees a selected kf0 however you reach the motion editor; wired into `rebindClipToTimeline` + `rebindMotionToSource`. **In progress next: clip editor B2 (slice + crossfade bake).**

**Clip editor B1 — bounce bake (Build 156, needs Daniel's verify).** Seamless-loop mode toggle (trim only / bounce / slice-disabled) + the bake pipeline. Bounce bakes forward+reverse via `exportVideo` (frameAt decodes source frames along a triangle source-time `q=1−|1−2p|`, re-encodes mp4), swaps in as the source (`applyBakedClip`), resets trim, re-binds. Destructive → `confirm` warning + progress bar. Limits: seek-based decode (slow on long clips), 30fps bake (no fps estimation yet), no mid-bake cancel. **NEXT — B2:** slice + crossfade bake (rearrange head→back + blend the B|A seam) + cut-point/crossfade-duration controls + in-editor seam loop-preview; reuses the same `bakeAndApply` pipeline (add the slice branch building the frame sequence with a blended seam). Then loop-lock; retime UX (single-select + 150%) + frame-interp; seek-reduction (footage cache).

**Perf diagnosis + clip-handle coalescing (Build 155).** Daniel's "M5 is slow" turned out to be Firefox-vs-Chrome (154 is snappy on Brave/Chromium) — Gecko's heavier seek/decode cost, NOT a regression; remedy is seek-reduction later (footage cache / decode-based thumbnails), not reversion. The one real engine-independent bug (clip-editor scrubber heavier than the main one, confirmed on Chrome): trim handles seeked per-pointermove without coalescing → decoder flood. Fixed via `clipSeekTo` (coalesced, mirrors `scrubVideo`). **Resolved:** 30fps (the tested baseline) is snappy on Chromium; Firefox slower = Gecko seek tax, not a regression. **Non-30fps (60fps) deferred** as a higher-risk hardening pass (see BACKLOG "Variable framerate / non-30fps source hardening" — also where the once-seen out-of-sync start/end keyframe is parked). **NEXT (unchanged):** clip editor B (baked bounce + slice/crossfade + destructive warning), loop-lock; plus retime UX (single-select menu + 150%) and frame-interpolation (priority raised); seek-reduction (footage cache) is the perf lever that helps Firefox + high-fps most.

**Clip editor increment A — trim + preview (Build 154, needs Daniel's verify).** `clip ▸` (video-only) opens `#clipSheet`: looping preview (own `<video>`) + two trim handles. `clip = { inT, outT, mode, slicePoint, crossfadeMs }`; `pToMediaSec(v,p,clip)` scales p into `[inT,outT]` (default = unchanged), `lockVideoDuration`/`videoNativeDurationMs` use trimmed length, `startVideoPlayback` loops within trim; trim resets on new video. **NEXT — increment B (bounce + slice/crossfade):** bake the slice (reuse export encoder) + destructive soft-warning + in-editor seam loop-preview. **Bounce DECIDED: bake it too** (Daniel) — both bounce + slice bake to a processed clip on Apply, so downstream is always a normal forward loop. B sub-split: **B1** bounce bake (decode trimmed range via seek-capture → assemble forward+reverse → encode via VideoEncoder+muxer → setSource blob → reset trim → re-bind), **B2** slice + crossfade bake + cut/crossfade controls + seam preview. Both behind the destructive soft-warning. Build B on a VERIFIED increment A (don't stack unverified).

**Video retime / playback speed (Build 153, needs Daniel's verify).** New video-only **speed** control (25/50/100/200%) in the motion footer. `motion.videoSpeed` → source `<video>.playbackRate` (preview) + locked `durationMs = native ÷ speed` (timeline + export stretch automatically; no engine change). `lockVideoDuration()` recomputes on motion entry / source switch / JSON load; `videoSpeed` in the motion JSON. Caveat: heavy slow-mo repeats nearest footage frames on export (no interpolation; fold masks it). **Queue:** loop-lock (last plan item) → pre-animation clip editor (BAKE for slice+crossfade, live remap for bounce; + looped transition-preview in the editor). Deferred: video color/rotation, long-render memory, out-of-view thumbnail refinement, slow-mo frame interpolation.

**Source-switch reset (Build 152, needs Daniel's verify).** Loading a new source mid-animation left stale state (old duration lock, old-source thumbnails, desynced play/pause; video also always free-ran). `loadVideo`/`loadImage` now halt playback + clear the filmstrip sig on any switch; in motion mode, `rebindMotionToSource` re-locks duration (video), resets playhead + view to fit, rebuilds thumbnails from the new source, and shows frame 0 timeline-driven. Keyframes are KEPT (source-agnostic — only source-dependent state was buggy). **Remaining queued:** loop-lock (kf0==end for a looping clip, last plan item) → then the pre-animation clip editor (trim + bounce/slice-crossfade; BAKE decided for slice, live remap for bounce). Deferred: video color/rotation pass, long-render memory, out-of-view thumbnail refinement.

**Fix: auto-spaced keyframe insert captures the interpolated value (Build 151, needs Daniel's verify).** Inserting `+keyframe` between two keyframes inherited the first one's look (it captured `{...state}` = the selected keyframe's snap). Now an auto-spaced insert captures `sampleKeyframes(others, tNew)` — the on-curve interpolated value — so the insert doesn't detour the motion. Anchored/kf0 adds unchanged. **NEXT (Daniel, 2026-06-09): pre-animation CLIP EDITOR** — a video-prep mode reached from motion mode's global controls (no kaleidoscope/keyframe UI): trim front/back, then make playback seamless via either BOUNCE (forward+reverse) or SLICE (cut point → move head segment to the back + crossfade). Plan in BACKLOG ("Pre-animation clip editor"). This supersedes the simple in/out trim item. Then source-switch reset + loop-lock.

**Tween band = aspect-locked cells (Build 150, needs Daniel's verify).** Replaced the stretched strip canvas with square `.mf-cell` thumbnails positioned by time (`makeStripCell`/`repositionStripCells`). Fit = edge-to-edge filmstrip; zoom spreads them crisp (no stretch); a debounced rebuild re-renders cells for the visible window `[pan, pan+span]` when idle (bounded seek count). Strip CSS transform is pan-only now (cells carry zoom). `buildFilmstrip`/`buildFilmstripVideo` reworked to the cell model; sig includes zoom+pan; rebuild triggers on zoom/pan settle. **Verify:** continuous at fit, crisp spread on zoom, re-fills on pause/pan, video cells show footage per time, no stretch. **Completes Daniel's 4 smoke-test notes** (Builds 148–150). **Next:** source-switch reset (bug), then loop-lock.

**Smooth playhead follow + pan-layer refactor (Build 149, needs Daniel's verify).** Replaces Build 148's disorienting page-jump: the playhead now pins at ~80% and the timeline scrolls continuously under it. Architecture change enabling cheap per-frame panning: markers + ruler positioned by **zoom only** (`zPct`) and panned via a **CSS layer transform** (`applyPan`); the playhead stays in the track (absolute, `tToPct`). `relayoutTimeline` (DOM rebuild) is now only for zoom/structural changes; pan-only (follow, two-finger scroll) uses `applyPan`. **NEXT (approved, Build 150): tween-thumbnail cell rework** — render thumbnails as aspect-locked square cells, increase SPACING on zoom instead of CSS-stretching, and re-render at the zoom level when idle (crisp fill). Plan: cells positioned by `zPct` inside `#mfStrip` (drop the strip's `scaleX`); cache the fit-level cells; on idle after a zoom, regenerate cells for the visible window.

**Three timeline refinements (Build 148, needs Daniel's verify).** From Daniel's smoke-test notes: (1) **playhead auto-follow** — `followPlayhead` pages the zoomed view so the scrubber stays on screen with lookahead during playback; (2) **context-aware `+keyframe`** — anchors at the scrubber when nothing is selected, auto-spaces after the selection when a keyframe is highlighted; (3) **ruler scrubbing** — dragging `#mfRuler` moves the scrubber. **Still open from those notes:** the tween-thumbnail rework — Daniel dislikes the CSS-stretch on zoom; proposed approach is to keep thumbnails aspect-locked and increase their SPACING on zoom (no stretch), then re-render at the zoom level when idle (crisp fill). That's the next timeline build, pending his go-ahead on the approach.

**Timeline zoom/pan/scale-to-fit (Build 147, needs Daniel's verify).** The long-pending "B4" — the timeline is now navigable for multi-minute clips. Ephemeral session view transform (`session.timelineZoom`/`timelinePan`); all time positioning routes through `tToPct`/`pctToT` (markers, playhead, ruler, scrub). The tween strip is CSS-transformed (`translateX`+`scaleX` on `#mfStrip`) so zoom/pan is instant and re-seeks nothing. Input (Daniel's pick): trackpad-native — ctrl/pinch wheel = cursor-centered zoom, two-finger scroll = pan; touch = two-finger pinch+pan; mouse = fit/±buttons in the transport row. `renderTimeline` = `relayoutTimeline` (no rebuild) + debounced filmstrip; zoom/pan only relayout. Entering motion mode resets to fit. **Verify:** zoom/pan/fit across engines; markers/playhead/ruler/scrub/marker-drag all correct under transform. **Known limits:** strip is CSS-stretched (soft at deep zoom — future crisp re-render-at-window); no playhead auto-follow during playback. **Milestone note:** this completes the core video-animation plan + timeline polish; could be the **v0.9.0** bump — left as a patch (v0.8.9) for Daniel to call, per his pattern of directing milestone version bumps. **Next:** source-switch reset, then loop-lock.

**Ruler shows total duration (Build 146, needs Daniel's verify).** Per Daniel's request, the ruler always renders the clip/loop length as a right-aligned, slightly-brighter (`#aaa`) end label; regular ticks within ~half a step of the end are suppressed to avoid crowding. **Verify:** right end always shows total time; no overlap with the last regular label.

**Filmstrip rebuild fast + flash-free (Build 145, needs Daniel's verify).** Fixes a Build-143 regression Daniel hit: Firefox crawled (offscreen `exportFrame`/`readPixels` is slow on Gecko, ×~15 cells) and Safari flashed through the cell renders (capture path borrows the live preview). Now the video filmstrip uses the readback-free **capture path on all engines** (fast GPU `drawImage`) and **freezes the preview behind a snapshot** during the background rebuild (`freezePreview`/`unfreezePreview` in main.js), lifted after a sync `engine.render(state)` so there's no flash. `IS_WEBKIT`/FBO branch removed. **Verify:** edit a keyframe over a video — no output flash on Safari, Firefox responsive again; band still fills over a beat on long clips (seeks). **Open perf lever if still slow:** a footage-frame cache keyed by (W, duration) so param-only edits rebuild seek-free (cell footage times depend only on width + clip length, not on keyframe params). **Also requested by Daniel:** the ruler should show TOTAL DURATION (an end label) — small follow-up (Build 146).

**Timeline ruler — ticks + timestamps (Build 144, needs Daniel's verify).** Second of the timeline-polish increment. A `#mfRuler` row above the track draws minor ticks + major ticks with timestamps at a width-aware "nice" interval (`renderRuler` in main.js, called from `renderTimeline` / resize / motion-entry rAF / duration onChange). `m:ss` past a minute else `Ns`. Inset 1px so ticks align with markers inside the track border. **Verify:** sensible label spacing on a short still loop and a multi-minute video; t=0 left-aligned; labels track the duration field (stills) / clip length (video). **Next in the increment:** source-switch reset (loading a new video keeps the old duration/thumbs/play-pause), then pinch/zoom/pan/scale-to-fit navigation — at which point the ruler + markers + strip + scrub all need to route through the zoom transform.

**Tween-strip band for video + flicker fix (Build 143, needs Daniel's verify).** First of the timeline-polish increment (Daniel's sequence: render tweened frames → ticks/timestamps → flicker → then nav). (1) The tween filmstrip band now renders for video: `buildFilmstripVideo` seeks the footage per strip cell as well as per keyframe (one ascending pass over time), builds into a fresh canvas, and swaps it in only when complete (old band stays up during rebuild). (2) Flicker fixed on Firefox/Chrome by rendering thumbnails OFFSCREEN via `exportFrame` (FBO → 2D scratch) instead of borrowing/resizing the live preview canvas; `IS_WEBKIT` gates this — desktop Safari's FBO readback is the "blue cells" corruption so WebKit stays on the capture path (its existing flicker remains, status quo). **Verify:** band fills with footage/tween across the timeline (≥2 kf); no preview flicker on Firefox during the debounced rebuild (Safari still flickers — expected). **Next in the increment:** time ticks + timestamps (Build 144), then source-switch reset, then pinch/zoom/pan/scale-to-fit. **Known cost:** the band is seek-per-cell, so it takes a moment to fill on long/high-res clips.

**Thumbnail-blank fix + drag-preview (Build 142, needs Daniel's verify).** (1) Build 141's thumbnails came back BLANK on Firefox because hiding the preview canvas during the build drops the WebGL drawing buffer on Gecko; removed the `visibility:hidden` — captures render now (cost: a brief preview flicker during the debounced rebuild, cosmetic). (2) Dragging a keyframe marker now scrubs the footage to the drop position live (`scrubVideo` in the marker pointermove), so you can place keyframes by eye instead of scrub-memorize-drag-blind. Possible follow-up to kill the flicker: render thumbs via FBO/`exportFrame` on non-Safari (Safari readback is the "blue cells" corruption, so it'd be browser-conditional).

**Async keyframe thumbnails for video (Build 141, superseded by 142 fix).** Keyframe marker thumbnails now seek the footage to each keyframe's own time before capturing (`buildFilmstripVideo`), so they're correct under add / edit / auto-shift / drag — fixing "editing one keyframe rewrites all the thumbnails." Async + single-flight (`_filmstripBusy`) + cancellable (`_filmstripGen`, bumped by `scrubVideo`/`startVideoPlayback`); preview hidden during the build, footage restored to the playhead after, bails if superseded. Adding a keyframe also seeks the footage to its new auto-spaced time. **Deferred:** the tween-strip BAND is skipped for video (would need a seek per cell). Expect a brief preview blank during the debounced rebuild. **Remaining timeline-polish:** source-switch reset (new video keeps old locked duration + can desync play/pause), time ticks + timestamps, pinch/zoom/pan/scale-to-fit, and the per-cell video tween strip.

**Keyframe-select seek fix (Build 140, verified).** `selectKeyframe` didn't seek the footage, so on a video source every keyframe you clicked/stepped rendered over the last-positioned frame (the most-recent edit "projected" its frame onto all keyframes). Now selecting seeks to the keyframe's time; `scrubVideo` gained `assignParams` so selection shows the keyframe's EXACT snap (not the interpolated value) over the correct frame. Covers marker-click, prev/next, post-delete reselect. NOTE: marker THUMBNAIL images are still stale (the separate async-filmstrip gap in the timeline-polish increment).

**Frame-accurate video export SHIPPED (Build 139, verified).** `exportVideo` now `await`s `frameAt`; a video source seeks the footage to each output frame (`advanceSourceToP`) before capture, so the clip advances through the render (no longer a frozen frame). Companion preview seeks too; footage restored to the playhead after. Seek-based ⇒ slow on long/high-res (one seek per output frame); the WebCodecs-demuxer accelerator is the future speedup. **NEXT recommended — timeline polish increment**, addressing Daniel's Build-138 findings: (a) **keyframe thumbnails are stale** — they don't seek to the keyframe's video frame, so they're wrong after auto-shift / drag-to-anchor (needs the async per-cell filmstrip + thumbs that seek); (b) **source-switch state bug** — loading a new video while keyframes exist keeps the old locked duration + stale thumbs + can desync play/pause (reset motion/duration/playback + rebuild on a new source); (c) **time ticks + occasional timestamps** on the timeline (now important for fixed-duration media); all alongside the **pinch/zoom/pan/scale-to-fit** work. Then 2b (loop-lock + in/out trim), then long-render memory.

**Stuck-frame scrub fix + v0.8.0 milestone (Build 138, needs Daniel's verify).** `seekVideoTo` no longer waits on `requestVideoFrameCallback`: our source `<video>` is occluded (opacity 0 behind the preview canvas), so on Blink/WebKit it never presents to the compositor → rVFC never fired → the seek promise hung → the coalescing scrub loop wedged on a stale frame (only play recovered, since playback reads `currentTime`). Now resolves on `'seeked'` (frame is ready for the texture) + a 2s safety timeout; `scrubVideo` clears its in-flight flag in `finally`. Same root cause as the source-overlay-not-animating symptom. Also: Firefox post-load `.mov` decode hiccups no longer false-blame ProRes (the codec hint only shows pre-load). **IMPORTANT:** video export is still **params-over-a-FROZEN-frame** — the footage does not advance per render-frame yet; **frame-accurate video export** (now unblocked by this fix) is the increment that makes the video actually move in the output, and is the recommended next step given Daniel's render interest.

**Source-video animation — increment 2a SHIPPED (Build 137, needs Daniel's verify): timeline binding.** Motion mode is un-gated for video. Entering it stops the free-run loop + pauses + locks `motion.durationMs` to the clip length; exiting resumes free-run. `shell/video-source.js` (`pToMediaSec`; `seekVideoTo` — rVFC-preferred, `'seeked'` fallback) + `advanceSourceToP` / `scrubVideo` (coalescing, latest-wins) in main.js. **Scrub** seeks the footage (params sampled at the same p); **playback** = the `<video>` as master clock (`startVideoPlayback`: play, derive p from `currentTime`, sample params, render). Duration field read-only for video. **Gaps:** filmstrip + keyframe thumbs don't seek per-cell yet (uniform strip across footage); loop-lock (kf0==end for a looping clip) + in/out trim = increment 2b; color/rotation quirks deferred. **NEXT:** 2b (loop-lock + trim) → async per-cell filmstrip → long-render memory (stream to disk) → frame-accurate video export.

**Source-video animation — increment 1 SHIPPED (Builds 133–136, needs Daniel's verify): load + play.** File picker + drop accept `video/*`; `loadVideo()` plays the video muted-on-loop and drives the kaleidoscope each frame via the live-camera render path (`startLiveLoop`). The source-slot PREVIEW is a 2D-canvas copy painted each frame via the source-overlay component's `paintSourceVideo()` (the component exposes `sourceVideo` through a `getSourceVideo` ctx hook; `mountSourceView` builds the canvas onto the component `view`). The `<video>` stays in the DOM occluded (opacity 0) so it keeps decoding + feeding the texture. Earlier builds showed black/blank source on Blink/Gecko (Safari-only) because `mountSourceView` reads the component `view`, which only proxied `liveVideo` — so 133 static-frame, 134 direct-`<video>`, and 135's canvas all fell through to the still `background-image`(blob-video-URL) path; **fixed in 136** by exposing `sourceVideo` + a `paintSourceVideo()` the loop calls. Motion mode is GATED OFF for a video source for now (`toggleMotionMode`/`updateMotionUI` check `env.sourceVideo`). Switching sources stops the loop + revokes the URL. **Known limitations (deferred, BACKLOG):** ProRes decodes only on Safari; iPhone `.mov` shows washed-out OUTPUT color (texture colorspace/range) on Safari+FF and a vertical aspect squish on Firefox (rotation/pixel-aspect metadata). **NEXT increment:** bind to the motion timeline — `session.videoSource` + `shell/video-source.js` (`pToMediaSec`/`seekVideoTo`/`advanceSourceToP`); un-gate + auto-engage motion; replace the free-run loop with timeline-driven scrub (coalescing seeks) + keyframes + loop-lock.

**Export throughput is browser-bound + single-threaded (Build 128 finding; `prefer-hardware` reverted).** Daniel's Safari timings: 4K ≈5 fps, 6K ≈2 fps, 8K ≈1 fps — all pegging ONE "Safari Graphics and Media" core, GPU idle, cost scaling with OUTPUT pixels (NOT source size; a huge source costs GPU memory + upload + keyframe-edit lag, but not encode throughput). The Build 127 `prefer-hardware` hint made ZERO difference (8K HEVC was ~1 fps in BOTH Build 126 and 127), so this path is CPU / color-conversion bound (per-frame canvas→`VideoFrame` + sequential encode), not encoder-selection bound — Safari isn't giving us hardware encode here regardless. Build 128 reverts the inert hint to restore the verified Build-126 encode config. **Implications:** the planned **worker** build makes slow renders non-blocking + abortable + memory-safe but will NOT multiply throughput (encode is sequential); real multi-core / hardware-encode (and Syphon) is the **native wrapper** (FOLD.md Phase 4). A possible browser-side win: skip the GL→2D copy (`VideoFrame` straight from the WebGL canvas) to cut a per-frame CPU color conversion — but that hung WebKit/iPad before (Build 115), so it's exploratory + needs a fallback. Build 129 shipped a per-frame timing reader (status + console: `gl` / `vframe` / `encode` ms). **Decisive result:** 4K H.264 — **Firefox 122 fps vs Safari 6 fps**, and the entire gap is `new VideoFrame(canvas)` (**Safari ~177 ms/frame vs Firefox ~5 ms**); `gl`≈0 and `encode`≈0 on both. So a basic worker (encode off-thread) will NOT speed Safari — encode isn't the bottleneck; it only buys long-render MEMORY + responsiveness. Real Safari speedups: a faster frame source (cheap, may not help) OR parallel `VideoFrame` creation across a worker POOL (the multi-core win). Chromium still untested. **RESOLVED (Build 131): per-engine frame source.** A/B results — desktop Safari WebGL-direct ≈130 fps @4K (>30 @8K) vs 2D ≈6 fps; Firefox + Chromium (Brave) fast on 2D, Firefox slightly *slower* on WebGL-direct. So: ALL WebKit (desktop Safari + iPad, Build 132) → WebGL-direct; Firefox/Chromium → 2D (`defaultCaptureMode` in main.js). iPad M1 tested at ~55 fps (vs ~18 on 2D), stable over a 75s render — the Build-115 hang is fixed on current iPadOS; `?capture=2d` is the safety hatch for any older iOS device. **Export throughput is no longer a bottleneck on any tested engine or device**, so the next build's disk-streaming is needed ONLY for long-render MEMORY (the speed motivation is gone, and the worker-pool idea is unnecessary).

**Video-export hardening (Build 126, verified by Daniel — works).** First build of the source-video animation track, but it's pure export work that also helps still animation. The **6K crash** was diagnosed as the H.264 level-5.1 4K ceiling (not the renderer): the render sheet now **gates resolutions per codec** (`pickVideoCodec` in `shell/video-export.js`) and only enables what the device can render (probed FBO ceiling) AND encode, routing >4K through **HEVC** (hardware on Apple Silicon/Safari; ≤4K stays H.264), adds an **8K** tier, and raises the duration cap **60s→600s**. FBO probe ladder gained 6144/3072. No new dependency (mp4-muxer already muxes HEVC). **Caveat:** long high-res renders can still OOM the in-memory muxer — Build 127 (OPFS streaming) is required before 10-min/4K is safe. **Verify:** 6K/8K appear + render on Apple Silicon Safari; disabled with a tooltip on Firefox/Chrome; ≤4K H.264 + 4K output unchanged; duration scrubs to 600s.

**Motion-entry keyframe seed (Build 125).** Entering motion mode auto-adds kf0 (current look, auto-selected); discrete (form/segments/mirror/OOB) locks at `>= 2` keyframes instead of `>= 1` (so a single seeded keyframe stays editable). New cross-browser backlog item recorded (Chromium pass — we've only tested WebKit + Gecko).

**Firefox motion-edit lag FIXED (Build 124, needs Firefox verify).** Root cause was a per-edit-frame / per-add keyframe-thumbnail `readPixels` (`fillThumb` → `exportFrame` → `renderToFBO`), which Firefox is much slower at than Safari. Removed `fillThumb`; thumbnails now come solely from `buildFilmstrip`'s readback-free capture path (debounced), which also handles a single keyframe now. (Build 123's `syncControls` coalescing was the wrong layer but is kept as a valid micro-opt.)

**Firefox overlay-drag lag (Build 123, needs Daniel's Firefox verify).** Coalesced the per-pointermove `syncControls()` to one rAF (`scheduleSyncControls`) in the source-overlay + output-gesture paths — it was layout-thrashing (per-event slider/textContent writes interleaved with the move handler's `getBoundingClientRect` read), which Firefox punishes much harder than Safari. If lag persists on Firefox: (a) cache the wrap rect at drag start for move/scale (rotate already does, Build 106) to drop the per-move `getBoundingClientRect`; (b) `preserveDrawingBuffer:true` is a known Firefox per-frame penalty but is required for the on-demand preview to stay visible — would need a continuous-render-during-drag or render-res-scaling approach to remove safely.

**Motion JSON + companion stroke weight (Build 122, needs Daniel's verify).** Companion video wedge lines thickened via `overlayStrokeScale` (live overlay untouched). Motion JSON round-trip: ⋯ menu in the motion footer (download/load motion data — source-agnostic `{format,version,app,durationMs,loop,smoothing,keyframes:[{t,anchored,snap}]}`), + "motion data .json (.zip)" render checkbox (bundles into the Build-121 zip). Load re-applies to the current source (thumbnails regenerate, `applyFormControls` syncs the form UI). **Verify:** download then load round-trips (incl. cross-form, smoothing/duration/loop); the render zip contains the chosen extras. The whole render-package cluster (source preview + JSON) is now done — remaining backlog: MediaRecorder fallback for non-WebCodecs, and B4 (timeline pinch-zoom/pan, still deferred).

**Companion source-preview video (Build 121, needs Daniel's visual verify).** Opt-in render-sheet checkbox renders a 2nd square 1920² video of the source + moving wedge (clean, no affordances) and packages both as a `.zip`. `exportVideo` is now generalized (`frameAt`/`onBegin`/`onEnd`). The frame compositor (`renderSourcePreviewFrame` in main.js) reuses `drawSourceOverlay` via the overlay view swapped onto an offscreen canvas. **Can't self-verify the visual** — check: wedge aligns with the source image, no affordance arrows, droste rings render, square fit looks right. **NEXT (planned with Daniel): motion JSON round-trip** — "download/load motion data" in a new ⋯ overflow menu in the motion footer + an "include motion JSON" render checkbox (reuses the Build-121 zip packaging: any extra selected → `.zip`). Source-agnostic JSON `{format,version,build,durationMs,loop,smoothing,keyframes:[{t,anchored,snap}]}`. Then B4 (timeline pinch-zoom/pan), still deferred.

**Blue-cells RESOLUTION attempt (Build 120, needs desktop-Safari verify):** root cause = desktop Safari's FBO `readPixels` returns corrupt (channel-swapped/banded) data; sync fixes (118 FBO-reuse, 119 fence-sync) both failed. Build 120 routes the **filmstrip** off `readPixels` to the `drawImage` capture path (`beginCapture/captureFrame`), synchronous, signature-skipped (no scrub rebuilds), and refreshes marker thumbs in the same session. The instant on-add keyframe thumb (`fillThumb` → `exportFrame` → `readPixels`) still uses the old path and self-corrects on the 600ms debounce — if Daniel still sees a brief blue on a just-added marker dot, move `fillThumb` to a readback-free path too (it can't resize the live preview, so: `drawImage` a warmed preview canvas, or defer entirely to the debounce). The fence sync (119) + FBO reuse (118) stay (harmless; help still-export robustness/GC).

**Blue-cells saga (needs Daniel's Safari verify):** Build 118 (persistent FBO reuse) did NOT fix it; Build 119 added a WebGL2 fence sync (`fenceSync`+`clientWaitSync`) before `readPixels` in `renderToFBO` — the symptoms (partial/banded/channel-swapped/flickering/self-correcting, repro on fresh webcam) match `readPixels` reading before GPU resolve, which `gl.finish()` fails to prevent on Safari. If 119 still doesn't fix it, the escape hatch is readback-free thumbnails (render→`drawImage` to a 2D canvas, like the video-export path) — but that can't resize the live preview, so it needs a dedicated offscreen target (keyframe thumbs could alternatively `drawImage` the warmed-up preview canvas).

**iPad export fix (Build 115, needs Daniel's iPad re-test):** Build 112's direct `VideoFrame(webglCanvas)` hung iPadOS Safari; now `captureFrame` GPU-blits the GL canvas into a 2D canvas and wraps THAT (Safari-safe source), keeping the speedup. If iPad still fails, ask Daniel for any console error — would pinpoint whether it's the VideoFrame source (now addressed) or the 4K canvas resize.

**Motion smoothing (Build 114, needs Daniel's in-browser pass):** velocity-continuous Catmull-Rom interpolation is now the baseline (motion flows through keyframes, no per-keyframe stutter); the "easing" control became a "smoothing" degree (0 = exact keyframes, higher relaxes jaggy keyframe values). Math in `kit/tween.js` `sampleKeyframes` (loop-aware, angle-unwrapped); verified numerically. **Validate:** a multi-keyframe one-direction zoom/pan should flow continuously (no stutter); a 2-keyframe A↔B loop should still ease smoothly at the turns; the smoothing slider should visibly relax sloppy paths. Pending: per-keyframe ease handles (deliberate holds).

**Video export perf (Build 112, needs Daniel's in-browser timing):** replaced the per-frame `readPixels`+Y-flip+`putImageData` with a direct **GL-canvas → `VideoFrame`** capture path (`engine.beginCapture/captureFrame/endCapture`), removing the single-core CPU bottleneck. **Validate:** a 4K loop should be dramatically faster than before; confirm output is correct (orientation, color, no corruption) and the preview restores cleanly after export. If timing is still bottlenecked, the deferred next lever is moving encode/mux to a Worker. (`exportAt` still + `exportFrame` thumbnail paths unchanged.)

**Keyframe model note (Build 111):** authoring reverted to **auto-select / duplicate-and-tweak** (undoes Build 97's "add deselects"). `+keyframe` always lays a keyframe after the current one and auto-selects it; edits write through (autosave) to the selected keyframe; an explicit synchronous commit-on-add prevents the stale-keyframe "pause" Build 97 was working around (Daniel diagnosed it as a missing save trigger, not auto-select). So the BACKLOG/CHANGELOG references to the Build 88/97 "add leaves nothing selected" model are now superseded.

**UI/UX polish pass in progress (Build 100+).** A small batch of presentation/coherence wins is underway, sequenced ahead of the higher-priority motion-perf work. Decisions with Daniel: **landscape mode is carved out** as its own build (needs a layout/IxD checkpoint first — seamless in-use rotation requires an in-place relayout that keeps the live camera alive, vs. today's reload-only chrome switch); the **motion-perf batch is interleaved** (first-keyframe lag → video-export render speed → motion smoothing → timeline pan/zoom). Plan: `~/.claude/plans/while-working-on-a-zesty-sonnet.md`.

- **Build 100 (shipped, Daniel confirmed default-mirror works on desktop + mobile):** default OOB → mirror; OOB demoted from a headed section to a labeled canvas control on both chromes; mobile settings regrouped into **slice → reset → canvas** sections with headings (driven by params' `scope` field).
- **Build 101 (shipped, needs Daniel's in-browser pass):** two fixes from Build 100 testing. (1) Desktop **canvas type ramp unified** — OOB and frame are now `.field`/`.field-label` blocks at the slider label's 12px/#888, so composition zoom / rotation / out of bounds / frame all read as siblings under the one "canvas" header (previously three different type rungs). (2) Mobile **popover float fixed** — the source/form menu now mounts inside `#m-context` with `bottom: 8px` instead of the viewport-anchored `--m-tabbar + inset + 8px` calc that double-counted the bar; **verify in the installed PWA** that the menu sits flush just above the tab bar.
- **Build 102 (shipped, needs Daniel's on-device pass — A2):** PWA tab-bar bottom anchoring, first iteration. Bottom padding trims the home-indicator inset by `--m-tab-clear` (14px, clamped ≥6px) so the bar sits closer to the true bottom; edge buttons round their bottom-outer corner by `--m-tab-corner` (20px). Both are `:root` vars in `src/mobile/styles.css` for fast tuning — **tell me how the clearance/radius feel on-device and I'll adjust.**
- **Builds 104–105 (shipped, needs Daniel's on-device pass):** mobile safe-area + overlay polish from Daniel's PWA testing. **104:** reverted the Build 102 nudge-down/corner-rounding (it overrode `env(safe-area-inset-bottom)` — fragile, native-app territory per Daniel) so the tab bar honors OS insets verbatim (also fixes "icons touching the edge"); tab bar respects side insets; **flip button** offset by `env(safe-area-inset-top/right)` so it clears the status bar / island. **105:** source/form popovers `position: fixed` and anchored off their launching button (`positionMenu()` — upward in portrait, leftward in landscape); save sheet capped at 600px + centered so it clears side islands. **Principle going forward (Daniel):** honor `env()` safe areas, don't pixel-match device corner radii / island geometry — that's for the eventual native iOS build with a fixed device set.
- **Build 103 (shipped, needs Daniel's on-device validation):** **mobile landscape layout — core.** Daniel pulled this forward from the carved-out/checkpoint plan and asked for the core horizontal relayout directly. `#m-root` flips column↔row via `@media (orientation)`; `layout()` + divider drag are orientation-aware; rotation relayouts in place (resize + matchMedia change) **without tearing down the chrome, so a live camera feed survives rotation.** Order L→R: OUTPUT | vertical divider | CONTEXT | vertical tab bar (right). Dynamic Island handled by `env(safe-area-inset-right)` (non-zero only when the island rotates onto the right edge → tab bar pads off it + 8px buffer; otherwise flush). **Validate on-device:** rotate during a live camera session (feed must not drop); island clearance when rotated clockwise (island right) vs counter-clockwise (island left); divider drag in landscape; canvas centering. **Known IxD follow-ups:** vertical tab-bar button feel, popover anchors to CONTEXT bottom (not near the tab bar), full-bleed corner-hugging. **Cannot be verified off-device** — `env()` insets are 0 in a desktop browser; relayout logic was reasoned through + builds clean, but Daniel is the validator.

**Video export SHIPPED (Build 96, v0.7.0) — needs Daniel's in-browser pass.** Render the loop to H.264 .mp4 via WebCodecs + **mp4-muxer** (a new, Daniel-approved dependency — the second authorized one after vite-plugin-pwa). Non-square output is now supported in the engine (`u_outputAspect` uniform; `renderToFBO(w,h)`; `engine.exportFrame`). The "render ▸" button in the motion footer opens a sheet (aspect 1:1/4:5/16:9, resolution 1080p/1440p/4K, fps 24/30/60). Untested by Claude (no WebCodecs here). Follow-ups: MediaRecorder fallback, 3K/6K, aspect crops for still export, video-file source loading.

**Mobile follow-ups from Daniel's Build 81 device testing (2026-06-02).** The PWA installs and runs on iPhone (chrome hidden, live camera works). Build 84 fixed three issues: touch-rotate runaway (suspected fix — **needs on-device confirmation**, couldn't reproduce remotely), the broken flip icon, and the tab popover toggle. The rest are queued in `BACKLOG.md` under "mobile chrome — Build 81 device-test follow-ups": mobile landscape mode (its own build, IxD), PWA tab-bar bottom anchoring (entangled with landscape), preserve source across a chrome switch, mobile settings ordering + OOB-as-canvas + slice/canvas headings, divider dock-snap, and the overlay min-size clamp.

**Versioning policy (Daniel, Build 80):** the VERSION **patch bumps every code-shipping build** now (alongside the monotonic BUILD), so each deploy advances `X.Y.Z` → `X.Y.Z+1`; minor/major still bump for milestones. Docs-only changes bump neither. (Codified in CLAUDE.md.)

## NEXT (fresh session): source-video animation track (plan approved)

We're building **animation of parameter keyframes over a source video** — the timeline becomes the video's own timeline (scrub to place keyframes), and a "this video loops" toggle drives the existing loop-lock (kf0 params == end). Full plan: `~/.claude/plans/we-just-finished-a-sorted-mist.md`. Scope settled with Daniel: full clip, up to ~10 min / 4K; **seek-based** frame extraction (universal codec support, incl. iPhone HEVC; the WebCodecs demuxer is deferred); **OPFS** streaming-to-disk for long renders (cross-browser); **HEVC** for 6K/8K where the hardware supports it (H.264 ≤4K elsewhere). WebGPU + demuxer + AV1 + audio + trim are backlogged with notes.

**Build sequence** (future build numbers left unpinned — perf detours keep shifting them):
1. **126 — export hardening (SHIPPED, verified).** Per-codec gating + HEVC for 6K/8K + 8K tier + 600s duration + FBO probe ladder.
2. **127 — `prefer-hardware` hint (SHIPPED, then reverted).** No measurable effect on Safari.
3. **128 — revert the hint (SHIPPED).** Restores the Build-126 encode config.
4. **129 — per-frame timing reader (SHIPPED).** `gl` / `vframe` / `encode` ms in the render status + console, to localize the throughput ceiling.
5. **130 — frame-source A/B experiment (SHIPPED).** Render-sheet selector to A/B the three `VideoFrame` sources; the data settled the per-engine choice.
6. **131–132 — per-engine frame source (SHIPPED).** All WebKit (desktop Safari + iPad) → WebGL-direct; Firefox/Chromium → 2D. Export speed solved on every engine + device tested.
7. **Next — long-render MEMORY: stream output to disk + size warning.** The in-memory muxer (~3 GB at 10-min/4K) OOMs; stream encoded chunks to disk (OPFS). Now that speed is solved, a worker is optional (responsiveness matters less) — likely main-thread OPFS `createWritable`, worker only if a browser needs it. Plus a pre-render size estimate/warning. (BACKLOG perf items 2–4.)
8. **133–137 — load video as source + bind to timeline (increments 1 + 2a SHIPPED).** Load + free-run play (`loadVideo`, `accept=video/*`, source preview = loop-painted 2D canvas via `paintSourceVideo()` since a textured `<video>` shows black on Blink/Gecko). Timeline binding (137): motion un-gated for video, scrub seeks footage (`scrubVideo` coalescing), playback = `<video>` master clock (`startVideoPlayback`), duration locked to clip. **Remaining sub-steps:** 2b loop-lock + in/out trim; async per-cell filmstrip. **Next increment — bind timeline + scrub:** `session.videoSource`; new `shell/video-source.js` (`pToMediaSec`/`seekVideoTo`/`advanceSourceToP`); coalescing seeks; un-gate + auto-engage motion for a video; replace the free-run loop with timeline-driven playback.
9. **Then — timeline pinch/zoom/pan/scale-to-fit** (the old "B4"; mirror `createOutputGestures`).
10. **Then — keyframing + playback + loop-lock over video** (capture/blit filmstrip, NOT `exportFrame`, to avoid the Safari readPixels "blue cells" corruption).
11. **Then — frame-accurate video export** (`exportVideo` awaits `frameAt`; flows through the disk-streaming pipeline; seam stays clean for the future demuxer).

**Core seam:** params (`sampleAt`) and source-time are independent functions of normalized p; everything reduces to one async `advanceSourceToP(p)` that puts the right decoded frame on the texture before the existing render+capture. That's why seek-based vs. demuxer is a localized swap, and the demuxer is a safe defer.

**Session arc just completed (Builds 100–125), for context:** the UI/UX polish pass (OOB→mirror default, settings coherence, type ramp), mobile landscape + safe-area robustness (honor `env()`, don't pixel-match — see the memory), tab-bar/menu/sheet positioning, the touch-rotate runaway (stacked listeners), the keyframe auto-select model + motion-entry keyframe seed, export speed (GL-canvas→`VideoFrame`, removed the single-core `readPixels`), motion smoothing (velocity-continuous Catmull-Rom + smoothing degree), the companion source-preview video + motion-JSON round-trip + zip packaging, and a string of **browser-engine** fixes (the "blue cells" = WebKit `readPixels` corruption escaped via `drawImage`; iPad export = WebKit `VideoFrame`-from-WebGL hang; Firefox motion-edit lag = per-frame `readPixels` removed). **Open browser gap: never tested on Chromium/Blink** — see the new item in the developer-tooling backlog (it lists the exact engine divergences to scrutinize). Deferred: MediaRecorder fallback for non-WebCodecs browsers.

---

### Animation phase build history (all SHIPPED) — kept for reference
1. **Tween/easing primitive — `src/kit/tween.js`. SHIPPED (Build 82).** `lerpState(a, b, t, easing)` interpolates two state snapshots; `tween.js` owns the canonical field classification (params.js can't — the direct-manip-only fields and `drosteSpiral` aren't declarative). Continuous fields lerp (`sliceScale`, `sliceCx/Cy`, `sliceRotation`, `canvasZoom`, `canvasRotation`, `squareAspect`, `drosteZoom`, `drosteSpiral`, `drosteOffsetX/Y`); discrete fields hold (`form`, `segments`, `drosteArms`, `oobMode`, `drosteMirror`, `drosteWedgeMirror`); the two angles take the shortest path around 360°. Easing: `linear`/`easeIn`/`easeOut`/`easeInOut` (default). No UI; invisible build.
2. **A/B loop playback (the doc's v1). SHIPPED (Build 83).** Motion-mode toggle (canvas group) → contextual transport footer: set/jump A & B, play/pause, loop, duration scrub, scrubber track. Driven by `lerpState` over a continuous rAF loop (modeled on `startLiveLoop`); playback renders interpolated snapshots transiently (working `state`/sliders/undo untouched). Gated to a still source, mutually exclusive with the live camera. Layout: `<body>` is now a column with the existing split wrapped in `.work-row`; footer hidden == identical to before. Plan: `~/.claude/plans/we-re-ready-to-start-silly-rabin.md`.
3. **Multi-keyframe timeline footer (desktop chrome). CORE SHIPPED (Build 87) — needs Daniel's in-browser pass; fast-follows pending.** Shipped: N-keyframe data model, the timeline track with saved-state thumbnails + scrubber + loop bookend, add/select/delete, play/pause/loop/stepper, total duration, explicit select-to-edit write-through, discrete locked to kf0. **Fast-follows:** drag-to-retime, pinch-zoom/pan + scale-to-fit, prev/next PiP monitors, per-segment rotation winding, gesture-record, fuller settings gating. **IxD-sensitive — Daniel drives the timeline UI.** **Detailed design direction is captured in `BACKLOG.md`** (animation track, 2026-06-02): Procreate-Dreams/iMovie north star; control areas (global transport / timeline track / keyframe-edit lane / scrubber); **loop-bookend** model (KF0 rendered at both start and end as the loop-return); **per-segment rotation winding** (default shortest-path, opt-in +N turns — revises Build 82's always-shortest-path); **output PiP comparison** (previous state vs current); motion-mode hides non-smooth controls; add-keyframe drops at the scrubber; pinch-zoom/pan + scale-to-fit; JSON project file. **Output toolbar STARTED (Build 86):** undo/redo + swap + motion moved off the output's edges to clear the bottom for the timeline. **Revised sequence:** timeline → video export → video loops → gesture-record → (discrete transitions only if compelling).
4. **Video export host module. SHIPPED (Build 96, v0.7.0).** WebCodecs `VideoEncoder` → mp4-muxer (H.264 .mp4); renders each interpolated frame via the FBO path (`engine.exportFrame`) with non-square aspect support. Aspect/resolution/fps sheet in the motion footer. MediaRecorder fallback (webm/vp9) is a follow-up.

**Decisions settled with Daniel (2026-06-02):** (1) **A/B prove-out first**, then generalize to a multi-keyframe list later; (2) **discrete params locked for the whole loop** (only continuous fields animate); (3) **loop = a toggle that tweens the end back to the start** (no manual loop-lock, no ping-pong). Default easing `easeInOut`. Still open for the multi-keyframe build: the timeline UI (Daniel drives), output aspect/fps for video export. **Verify:** a 2-keyframe loop interpolates smoothly and loops seamlessly. Full Phase 3/3.5/4/5 framing is in BACKLOG ("animation + performance track") and the plan doc `~/.claude/plans/i-d-like-to-think-parsed-sloth.md`. When delivering a new build, increment BUILD by 1 and bump VERSION when meaningful change ships. **BUILD never resets** on version bumps — it's a global monotonic counter (see `version.js` comment).

## what's working

The full kaleidoscope app is functional and tested. Three forms (radial, square, hex), full slice + canvas controls, direct manipulation on the source overlay, export at 1K through GPU-max, all OOB modes, drag/swap/divider, scrub fields with pointer lock, slider sync.

Daniel has tested Build 19 and reports core functionality "all working great." Build 20 added docs and license. Builds 21-23 are an iPad touch pass. Build 24 adds session undo/redo: divider touch + wider hit target, coarse-pointer slider thumb sizing, overlay grip-line affordance removed, overlay two-finger pinch for slice scale + rotation + repositioning (midpoint of fingers drives position), preview canvas two-finger pinch for canvas zoom + rotation, canvas zoom min lowered to 0.15, GPU FBO size probe with 2D canvas encoding check (fixes export failures on iPad).

## current state of the architecture

Vite project, single static-site bundle. Engine in `src/engine/`, shell in `src/shell/`, single `src/main.js` entry. Forms registry pattern: each symmetry form is a self-contained module in `src/engine/forms/`. Adding a new form = one new file + one line in `forms/index.js`.

Read `ARCHITECTURE.md` if you need details on the registry, shader composition, or `env` runtime container.

## what we're doing right now

Build 57 addresses five follow-ups from Build 56 testing:

1. **Source Y-flip fixed globally** in `toSourceUV` — invisible on mirror-symmetric forms, visibly corrects Droste at arms=1 (no more upside-down output).
2. **Export spinner now visible** — removed `setBusy()/clearBusy()` from `doExport` since the fullscreen busy overlay was covering the button's spinner.
3. **Combined diamond + dot into one handle.** Removed `drosteShiftX/Y` state and `u_drosteShift` uniform. The diamond's parameter (`drosteOffsetX/Y`) now drives both the Möbius pre-composition AND the source-side per-tier drift simultaneously. One handle, two effects.
4. **Wedge mirror at arms=1: GLSL no-op + UI hidden.** Removed the Build 54 tier-parity theta mirror block. The wedge-mirror row is hidden from the slice panel when `state.drosteArms === 1` (it's meaningful only at arms ≥ 2).
5. **Smooth spiral seam preview** — when spiral > 0 at arms=1, a smooth log-spiral curve traces the tier-0/tier-1 seam in the source overlay (80 line segments, no jagged silhouette).

Planned next builds:
- **Build 58+:** true vanishing-point offset (per-tier rigid translation; no in-tier distortion). Backlog. "Dimensional rotation" (volumetric tilt where each tier projects at a different angle) backlog.

**What Daniel needs to verify in-browser for Build 57**:

1. **Y-flip fix at default state** (Droste, arms=1, spiral=0, mirror on): upload a clock image. Numbers should appear right-side-up (12 at top), no longer mirrored.
2. **Other forms unchanged**: switch to radial/square/hex/triangle. Outputs should look the same as Build 56 (the Y-flip is invisible under kaleidoscope mirror symmetry).
3. **Export spinner**: click export. Button shows spinner + disabled state; status text below says `rendering …`. **No** fullscreen busy overlay.
4. **Combined offset**: drag the diamond. Expect both the spiral pole shift (rings nest off-center) AND deeper-tier source content drift toward the offset direction. Single handle, no separate dot.
5. **Wedge mirror toggle**: at arms=1, the wedge-mirror row should be HIDDEN. Increase segments to 2+, the row reappears.
6. **Spiral preview**: at arms=1, spiral=1, a smooth log-spiral curve overlays the source thumbnail showing the tier seam. Adjust spiral slider, curve updates smoothly.

Still pending from prior builds: Intel Air investigation (blocked on hardware access). Triangle form still pending production review of `TRI_SIZE`, `tilesPerDim`, and Build 37 fold-transform side effects.

**Strategic sequencing session (2026-05-31):** worked through the multi-version architecture with Daniel and rewrote the `BACKLOG.md` capability tier. Key outcome — "shell" was conflating four layers (Engine / Kit / Host / chrome); only the front-end chrome is genuinely rebuilt per use case, everything below is shared and composed. There are **two front-ends** (desktop, extended; mobile, new) plus a deferred MIDI/kiosk third. Camera is a host module wired into both. The agreed build order is Phase 0 (parameter registry + texture-source spike) → 0.5 (camera host + desktop/iPad wiring) → 1 (mobile still-editor chrome) → 2 (camera in mobile) → 3 (tween kit + still-animation) → 3.5 (random/live-wallpaper) → 4 (video-file animation) → 5 (live motion + Syphon external output). See the `BACKLOG.md` capability tier for the full reasoning.

**Phase 0 — parameter registry: SHIPPED (Build 65).** `src/shell/params.js` is the declarative catalog; the 6 clean sliders (`scale`, `compZoom`, `sliceRot`, `aspect`, droste `zoom`, `canvasRot`) now wire from it via a loop over `DECLARATIVE_PARAM_IDS` through the unchanged `wireSliderWithScrub()`. Stateful controls (segments form-routing, arms-aware spiral fmt+snap, mirror/wedge-mirror/OOB toggles) stay bespoke with catalog-only `PARAMS` entries. No behavioral change intended.

**What Daniel needs to verify in-browser for Build 65:** the registry refactor should be invisible. Exercise every slice + canvas slider (scale, rotation, composition zoom, aspect on square, thickness/spiral on droste, canvas rotation) — ranges, scrub fields, formats (`×`, `°`, p/q spiral fractions), and undo/redo should behave exactly as Build 64. Confirm form-switch show/hide and the droste toggles still work, and that export is unchanged.

**Phase 0.5 — live camera (host capability): SHIPPED (Build 66).** Camera wired into the desktop/iPad chrome. The engine now accepts a `<video>` source (`setSource`/`updateSourceFrame`/`getSourceSize`/`clearSource` in `engine/index.js`; `updateTexture` in `gl.js`); `src/shell/camera.js` is the host module; `main.js` adds the continuous live loop + camera UI; `overlay.js` mounts the live `<video>` in the source view. Capture = freeze + save both (raw + kaleidoscope), stay editable. This also satisfied the Phase 0 texture-source spike (video works as a real source).

**What Daniel needs to verify in-browser for Build 66** (camera path is build-clean but unverified by Claude — getUserMedia needs a real browser + permission):
1. **Start camera:** click "use camera" (allow permission). Live kaleidoscope should animate from the rear camera; the wedge overlay sits on the live feed in the side slot and is draggable.
2. **Controls live:** form switch, segments/scale/rotation, composition zoom etc. all affect the live output in real time.
3. **Flip:** "flip" switches front/rear; front preview is mirrored.
4. **Capture:** "capture" downloads TWO files (the kaleidoscope at the chosen export size + `…-raw.png` at native res), freezes the frame as the editable still, and stops the camera — you can then fine-tune and re-export normally.
5. **Stop:** "stop" returns to the empty placeholder.
6. **iPad:** confirm the whole flow on iPad (the intended capture surface). Needs https (or localhost) — a LAN IP without https will show a secure-context error.

**Build 67 — fixes from Daniel's first iPad camera test (the camera works great on iPad — big milestone).** Addressed: version string (now v0.4.0), front-camera sampling the mirror-opposite side (texture now mirrored to match preview), captured thumbnail disappearing (blob URL kept alive + `env.liveVideo` handle), export spinner regression (double rAF), and capture no longer auto-saves. New capture/export model: **capture freezes the frame as the editable still and saves nothing; the first export saves the unmodified original (raw frame) alongside the kaleidoscope; later exports of the same source save only the kaleidoscope.** Desktop now defaults to the front camera (mirrored); iPad keeps rear default.

**Needs Daniel's re-verification on iPad + desktop:** front-camera sampling now matches the wedge position; captured thumbnail persists and stays editable; export shows the spinner and (first time after a capture) downloads two files; desktop opens the front camera mirrored. Watch the two-file export for browser "multiple downloads" prompts.

**Build 68 — export rework + overlay refinements (Daniel's pre-mobile cleanup).** Export is now two buttons: "export composition" (single file) and "export package (.zip)" (composition + unmodified original in one zip — replaces the Build 67 two-download approach that Safari collapsed to one file). New dependency-free `src/shell/zip.js` (store-only, validated with `unzip -t`). Square form draws one affordance cluster (top edge + right edge + top-right corner + rotate beyond the right edge) instead of all 8; hit-testing unchanged. Rhombus scale band trimmed to a thin interior (4px) + 16px exterior so the interior is mostly a move target.

**Needs Daniel's re-verification:** square shows only the one cluster and rotates cleanly; rhombus has room to move vs scale at small sizes; "export package" produces a valid zip on Safari/iPad with both files; "export composition" is a clean single download.

**Next phase: 1+2 — Components extraction → mobile chrome + camera (IN PROGRESS, approved 2026-06-01).** Refined layer model is now **Engine / Kit / Components / Chrome**: the mobile chrome is NOT a duplicate UI — it mounts the SAME components desktop uses, parameterized not forked. The expensive source-overlay event/proportionality/mirroring math must never be reimplemented per chrome.

Sequence: **(a) extraction pass first**, behavior-preserving, with the current build (incl. **iPad live camera**) as a byte-identical oracle — move snaps to `src/kit/snaps.js`; extract `src/components/source-overlay.js` (`createSourceOverlay(ctx) → {render, destroy}`) and have desktop `main.js` consume it; extract output gestures (`setupPreviewGestures`) to `src/components/output-gestures.js`; extract a registry-driven `mountRangeControl` to `src/components/param-control.js` (mobile-facing; desktop slider DOM stays static this pass). **(b) mobile chrome:** `src/boot.js` mode-detect (phone → mobile, else desktop incl. iPad; `?chrome=` override), `src/mobile/chrome.js` (OUTPUT top + CONTEXT panel bottom + fat sticky grippy divider; context flips SOURCE↔SETTINGS; bottom tab bar with SOURCE/FORM popovers, contextual capture toggle, EXPORT sheet). **Decisions:** mobile opens to **source-picker/empty** (not camera-first); divider has a **soft center detent only**; one-finger output drag is **no-op v1**. **(c) PWA** via vite-plugin-pwa (authorized); verify `getUserMedia` in iOS standalone on Tardigrade.

Full plan in `~/.claude/plans/i-d-like-to-think-parsed-sloth.md`. The export-package zip remains the seam for overlay/geometry export layers (BACKLOG).

**Mobile chrome: at parity + PWA (Builds 70–80).** Core + live camera + icon tab bar (source/form popovers, capture/go-live, flip) + full settings (incl. stateful segments/spiral/mirror toggles + OOB) + the save sheet (diagnostics/format/size/save-package/save-composition + lazy 8192 `probeMaxFBOSize` + res hint) + portrait fill/fit toggle + **PWA** (vite-plugin-pwa: manifest, offline SW, iOS standalone metas, `public/fold-icon.svg`). **On-device verification still needed:** (1) installed standalone hides Safari chrome and the tab bar sits clean; (2) **does `getUserMedia` work in an installed iOS standalone PWA** (historically fragile); (3) real PNG app icons (SVG icon → iOS home-screen falls back to a screenshot). PWA `base` is relative (`./`) — if the SW misregisters on deploy, switch to `/`. Mobile undo/redo stays out of scope. Remaining roadmap is the animation track (Phase 3+, desktop-first) in BACKLOG. Below is the earlier Build-70 detail:

**Mobile chrome status (Build 70):** first increment shipped — the still-editor core. `src/boot.js` selects chrome (phone → `src/mobile/chrome.js`; iPad/desktop → `src/main.js`, unchanged). Mobile mounts the shared components (`createSourceOverlay`, `createOutputGestures`, `mountRangeControl`) in a two-region + sticky-divider layout with a SOURCE/SETTINGS context flip and a minimal tab bar (upload/form/export). **Test it:** load with `?chrome=mobile` in a narrow window, or on a phone — upload an image, drag the wedge (move/twist/pinch), flip to SETTINGS and adjust sliders, cycle the form, export. **Confirm desktop + iPad still load normally through boot (behavior unchanged).** Next increment: camera wiring + SOURCE/FORM popovers + EXPORT sheet + stateful settings controls, then PWA. This is IxD-sensitive — Daniel should react to the layout before it's built out further.

**Extraction pass status (Build 69):** Steps A–C done and building clean — `src/kit/snaps.js` (droste snaps), `src/components/source-overlay.js` (`createSourceOverlay`, desktop consumes it), `src/components/output-gestures.js` (`createOutputGestures`, desktop consumes it). The `overlay.js` drawing/hit-test/gesture bodies are UNCHANGED — only desktop wiring moved — so the intent is byte-identical behavior. **Claude could only build-check; Daniel must verify byte-identical on desktop + iPad before the mobile chrome builds on these:** every overlay gesture (move/scale/rotate/segments/square edge+corner/droste ratio+arms+offset/two-finger pinch), output pinch/twist, form switch, swap, divider resize, undo/redo, export, AND the full iPad live-camera flow (start/flip/capture/package) — all should match Build 68 exactly. Step D (registry-driven `mountRangeControl`) is deferred into the mobile-chrome step (it's mobile-only, can't be verified byte-identical on desktop).

`docs/FOLD.md` owns vision, brand, marketing narrative, monetization paths, and gallery show concept. `docs/BACKLOG.md` capability tier now carries the layered vocabulary and the Phase 0–5 sequence; mobile UX exploration notes, gallery installation work, and open architecture questions sections are present.

## decisions locked in

- **License:** AGPL-3.0, copyright Daniel Nelson. The author retains rights to commercial licensing. This was chosen over MIT to discourage forking-as-competitor while keeping the code openly viewable.
- **Repo:** public.
- **Build counter convention:** monotonic global, never resets on version bump.
- **Docs structure:** `README.md` at root, `docs/HANDOFF.md` `BACKLOG.md` `CHANGELOG.md` `ARCHITECTURE.md`.
- **Form ID is a string** (not numeric index) everywhere. Don't reintroduce numeric form indexing.
- **The `env` runtime container** is the seam between shell modules. Don't add module-level mutable globals; thread state through `env` instead.

## decisions deferred

- **"Scale to tile" canvas zoom snap.** Build 19 conceptual analysis concluded it's feasible only for square output, but Daniel reports visually-repeating patterns appearing at certain zoom-out levels and wants to revisit. Deferred until someone has time to investigate with screenshots. See `BACKLOG.md`.
- **Monetization approach.** Full narrative and phased plan now in `docs/FOLD.md` under "monetization paths." The AGPL license preserves all options.

## what to avoid

- **Don't reset BUILD when bumping VERSION.** It's a monotonic global counter. Read the comment in `src/version.js` if unsure.
- **Don't put backticks inside form GLSL strings.** The `glsl` field in form modules is a JS template literal; a backtick inside breaks parsing silently. The original monolith had a long-running bug from this. (Mentioned in `ARCHITECTURE.md` too.)
- **Don't assume Daniel sees what you describe.** He's caught Claude hallucinating UI elements before (e.g. a "Clip" transport mode option that didn't exist in his Resolume version, in another project). When describing Resolume / Vercel / VS Code UI, be tentative and defer to what he actually sees on screen.
- **Don't introduce new mutable module-level state in shell modules.** Thread it through `env` instead. The `_windowHandlers` and `_overlayDrawPending` patterns from the original monolith have already been ported to env-based equivalents.

## environment / hardware

- M1 Max MacBook Pro
- 500GB WD Black NVMe SSD (USB 3) — used as project drive in some VJ workflows; not relevant to kaleidoscope but noted because it came up
- Akai APC40 MK2 — relevant only for the future "live shell"
- iPad — primary touch target post-deploy, untested until on a public URL
- Browser: Chrome primary

## context from prior sessions worth preserving

Daniel was learning Resolume in parallel with the early kaleidoscope work, and there's a separate `drift` project (a video-art PWA) that shares some architectural DNA but is unrelated functionally. The handoff for Drift mentions "plans to open-source on GitHub" but no license was actually picked there — kaleidoscope is the first of his projects to land on AGPL-3.0 explicitly.

If Daniel asks Claude to look at Drift or Zoetrope (another project of his), they're available in the project knowledge as separate handoff docs.
